# Implementation evidence

The full objective remains completing TODO.md, including M1–M6 and both consumer migrations. The repository began with only the spec, plan, and Nix environment. No release has been published and no external application has been modified.

## Current qualification

- Completed the M2 authentication-layout line. The dedicated `@gemologic/sheen-patterns/auth` entry now exports a deterministic AuthLayout base plus focused OAuth/OIDC and brand-split starters while leaving provider discovery, requests, errors, redirects, authorization, and sessions application-owned. The split presentation uses logical placement and a CSS-only narrow collapse, so the same main, form, input, native draft, and Solid owners survive resize and delayed hydration. Loupe includes both full-page starters, links them from the landing page and gallery, and renders all three components as live bounded catalog previews. Four focused SSR/validation cases, four Chromium interaction/responsive/hydration/catalog cases, and both route-derived axe cases pass with three reviewed dark snapshots. The 182-component/186-example manifest, patterns and Loupe builds, workspace typechecks, focused lint, package contents, and isolated consumer checks pass; the auth entry measures 2,058 bytes gzip under its 15,000-byte gate without AppShell, AdminApp, table, chart, date, or Composer code. A live desktop review reports Lighthouse accessibility and best-practices scores of 100. WebKit and manual assistive-technology qualification remain release-wide work.

- Fixed the FileDropzone's zero-width intrinsic layout on the generated component index and playground. FileDropzone now fills its containing block, and a focused Chromium regression checks both documentation surfaces in addition to the existing picker, drag, upload, hydration, and axe coverage. A separate production catalog measurement confirmed that all 179 previews are real server-rendered components, but exposed one repeatable 233–245ms Long Task when the first application-pattern cards become visible. Package consumers do not load this catalog; bounding Loupe's deferred preview work remains a documentation-site launch task.

- Completed the Studio application-visual pass. The seventh bundled theme adds calm near-black/cool-white surfaces, a slightly airier content type scale, and Phosphor artwork while keeping dark Obsidian as the fresh-install default and radius as an independent axis. AdminApp now defaults its general chrome to comfortable density; DataTable exposes a validated local `density` prop, invalidates virtual measurements when that local value changes, and the heavy starter plus generated Composer output opt tables into compact without shrinking navigation, dialogs, or content. Admin action wrappers preserve current color, fixing the New account glyph. The collapsed Admin rail now uses the specified 48-pixel width, forces every nested destination onto one centered track, hides row action menus until the rail is expanded so their hit targets cannot cover navigation, and preserves the rail preference across starter-route navigation. Live review exposed a nested-theme icon bug where both Radix and Phosphor SVGs matched different ancestors; inherited nearest-scope display variables now select exactly one glyph in scopes and portals. Sixty-seven focused unit/Composer cases, relevant builds, manifest, lint, typechecks, consumer and package-size gates pass. The final production Chromium Admin run passes all 58 hydration, navigation, keyboard, axe, forced-colors, reflow, responsive, and heavy-workload cases. The production icon and full 168-combination contrast runs also pass, as does the local-density transition. Long same-page audit loops initially exhausted Chromium's resource pool; independent state and keyboard cases now preserve the coverage without retaining obsolete page resources. WebKit and manual assistive-technology evidence remain release-wide work.

- Corrected the FileDropzone presentation after a live narrow and desktop review showed its target collapsing into a one-line toolbar. The complete native-picker label now owns a full-width density-aware drop surface; helper text activates the picker, status rows use a stable two-level layout, compact containers stack actions, and automatic content direction keeps English filenames and punctuation intact inside RTL scopes. Hover, drag, and focus states use their actual fill as the focus-ring offset. The six focused Chromium cases pass after adding picker-surface geometry and instruction-activation coverage; the final 375px and desktop views were reviewed, and the mobile snapshot retains Lighthouse accessibility and best-practices scores of 100 with no component errors.

- Added a non-publishing clean npm consumer gate for release candidates. It creates real tarballs for all ten fixed-version packages, installs them together through npm outside the workspace, validates the complete dependency tree and high-severity audit boundary, typechecks public entries, imports Node token/CLI/lint entries, and builds both compiled and Solid-source Vite graphs across representative UI, icon, date, table, pattern, chart, and code exports. The final run passes at `0.0.0`; all tarballs remain temporary and nothing was published. The release guide now separates first-package bootstrap from subsequent OIDC staged publishing and keeps non-inherited Solid/Kobalte patches as an explicit stable-publication blocker.

- Completed the non-publishing M6 release-infrastructure line. Changesets 3.0.2 now keeps all ten public packages on one fixed version line, requires a reviewed or explicit empty Changeset on pull requests, and drives a dedicated version-PR workflow with no publish script, npm credential, Git tags, or GitHub release path. Every package has public-provenance and repository metadata, its own concise README, and a byte-identical Gemologic MIT license. Ordered negative `files` globs preserve Solid source exports while excluding tests, demos, and component metadata from tarballs. A real `pnpm pack --dry-run --json` gate inspects 1,030 files across all packages, resolves every export/bin target, and additionally requires the IBM Plex source/OFL artifacts plus the CLI skill and application patches. Two focused release-configuration tests, workspace typechecking, a complete production build, Nix formatting/evaluation, and exact realization of the new `sheen-package-check` Pog wrapper pass. Changesets parses the configuration locally but cannot calculate divergence in this all-untracked checkout because no committed `main` ref exists; hosted version-PR execution and actual publication have not been claimed. The documented deprecation/codemod policy requires one minor overlap, type and metadata guidance, idempotent previewable codemods for mechanical changes, and explicit manual migrations for product decisions.

- Completed the remaining automated M4 adaptive-accessibility and application-performance lines. The Composer hostile fixture now combines forced colors, reduced motion, multilingual RTL phone content, 200%/400%-equivalent reflow, focus-not-obscured checks, and WCAG 2.5.8 target qualification. It exposed two real compact-size defects: compact small controls were 22px high and long filter-chip text could squeeze its remove target below 24px. Compact small controls now have a 24px floor and filter removal retains a fixed 32px allocation with a 24px minimum. The focused qualification passes. A new five-context calibrated Composer benchmark covers title configuration, add/remove, theme and viewport changes, and query editing while retaining surviving editor, iframe, AdminApp, table, QueryBuilder, row, and draft owners. Its independent repeat passes the committed local baseline at about 16.8ms p99 with zero frames over 50ms, Long Tasks, unexpected layout shifts, or required owner loss. All benchmark families now fail on three consecutive increases, matching the accepted specification instead of warning. This is local Chromium evidence, not hosted-runner, WebKit, NVDA, or VoiceOver proof.

- Completed the M5 CLI mutation-safety line. Real temporary-repository tests prove `new` dry-run writes nothing, ordinary creation refuses overwrite, force prints a diff before replacement, and `sync-skill` changes only its owned clean directory while rejecting modified, untracked, and ignored contents. Twelve focused CLI cases pass. The pinned twenty-prompt agent assay still lacks a baseline. Codex CLI 0.153.4 with `gpt-5.6-sol` at xhigh reasoning hit the repository's five-minute ceiling after 72,143 tokens on the first prompt and produced no artifact. A medium-reasoning diagnostic completed six of seven attempted prompts, but the remote-orders prompt was stopped at the same ceiling after 79,635 tokens without an artifact. Three completed modules exposed a shared `exactOptionalPropertyTypes` failure, so the repository profile, compact context, vendored skill, and generated-app guidance now tell agents to omit absent optional JSX props rather than pass `undefined`. The failed configuration will not be retried without user direction.

- Completed the deterministic half of the M5 assay gate without weakening its missing-baseline state. A strict metadata parser and documented JSON schema require the exact CLI/model/reasoning/sandbox/timeout capture, five context identities, twenty prompt-addressed raw outputs, reports, and aggregate. Verification rejects path escape, duplicate/missing IDs, context or artifact digest drift, malformed raw output, inaccurate generated-line reports, any scoring failure, and aggregate mismatch. A source auditor additionally typechecks each untouched TSX module against the real public-package consumer, applies repository ESLint, rejects private dependency imports, and derives interaction coverage and manifest-invalid props rather than trusting those report fields. The normal `pnpm check` invokes the verifier conditionally; it becomes active automatically when the first baseline exists, while the direct command fails closed when it does not. Six focused tests cover complete verification, context/output/report tampering, real public imports, manifest props, exact-optional type failures, and private dependencies. This is gate infrastructure, not the missing model baseline, so both assay TODO lines remain open.

- Completed the M3 TimeSeries/uPlot lifecycle line. The dedicated `@gemologic/sheen-charts/time-series` entry progressively enhances a deterministic, command-bounded SVG server fallback inside an exactly reserved plot region, while retaining the figure, narrative, plot host, native table disclosure, and rows through hydration. UTC epoch milliseconds stay in millisecond units, finite typed columns are passed without copying, and only columns containing NaN are translated to uPlot's explicit null-gap form. Scoped theme changes redraw the existing canvas, accepted refreshes use the existing uPlot owner, ResizeObserver work is frame-coalesced, and disposal destroys the renderer and releases its shared token probe. The typed `__unsafe_uplot` seam exposes only non-owned engine options. Three focused SSR/type cases and seven Chromium cases pass for twenty-frame accepted refresh continuity, exact failed-refresh retention, theme redraw, responsive sizing, cleanup/remount, delayed hydration geometry and identity, axe, and a reviewed dark baseline. Live Chrome DevTools exposed and drove a compact visual-axis formatter fix; its final review reports the expected figure/image/disclosure tree, Lighthouse accessibility 100, no component console warnings or errors, 69ms local LCP, and zero layout shift. Installed production consumers measure the isolated TimeSeries plus chart CSS and uPlot at 34.1KB gzip under a 60KB gate, while core/SVG/table and tree-shaken root-hook consumers retain no chart engine. WebKit, manual assistive technology, advanced chart interactions, and calibrated high-volume chart benchmarks remain separate unfinished gates.

- Completed the M2 SplitLayout line and the grouped SettingsLayout/SplitLayout inventory. SplitLayout composes one start/end content pair into named ScrollArea regions and the existing Corvu-backed Resizable separator, accepts controlled or local fractions, delegates asynchronous persistence to an app-owned adapter, and exposes retained refresh state without hiding either pane. A component container query stacks horizontal panes in narrow space, removes only the separator from presentation, and restores the same pane, input, and content owners when space returns. The work exposed a controlled Resizable defect: callbacks received new fractions while a non-reactive option spread left Corvu at its server geometry. Resizable now drives a reactive validated size value and correctly snaps rejected controlled proposals or accepts parent updates. Six focused Resizable/SplitLayout SSR cases and seven combined Chromium cases pass for real endpoint persistence, keyboard resize, wide/narrow handoff, exact delayed-hydration geometry, an early native draft, twenty-frame refresh continuity, retained nodes/focus, axe, and forced-colors boundaries. Live Chrome DevTools reports the expected two-region/separator tree, Lighthouse accessibility 100, and no console warnings or errors. UI/patterns builds pass; inventory-wide static and consumer gates are rerun at this boundary. WebKit and manual assistive-technology qualification remain release-wide work.

- Completed the M2 CodeBlock extension line. The basic renderer now supports a filename/caption header, visible language, exact-source Clipboard action with localized success/failure feedback, controlled or uncontrolled wrapping, line numbers without a highlighter, and validated one-based highlighted-line ranges with an assistive description and forced-colors boundary. Plain and tokenized source use stable index owners so wrapping, mode changes, and same-shape accepted source refresh do not replace the preformatted region or surviving line nodes. Shiki remains confined to the optional `@gemologic/sheen-code/highlight` entry; the isolated renderer is 3.17KB gzip under its 5KB gate and retains no Shiki engine. Three focused SSR/validation cases and five Chromium cases pass for exact native clipboard bytes, complete server markup, controlled wrapping, retained nodes/focus, syntax-theme updates, and axe. Live Chrome DevTools reports the expected filename/actions/code description tree, Lighthouse accessibility 100, and only the existing development font-preload warnings. UI/code builds, workspace typechecks, lint, and isolated consumer gates pass. The generated inventory is rerun at this boundary; WebKit and manual assistive-technology qualification remain release-wide work.

- Completed the M2 AccountMenu, WorkspaceSwitcher, and NotificationCenter gallery line. Loupe now renders topbar, sidebar-header, and scoped RTL sidebar-footer placements against app-owned state and real GET/POST endpoints; the package components contain no network or persistence owner. NotificationCenter adds localized empty, unread, and mark-all copy plus visible unread text, and its stable ID renderer retains an open item, trigger, and focused native destination during accepted record refresh. Five focused Chromium cases and the complete 16-case AdminApp regression pass for account/workspace actions, notification read state, scoped portals, delayed hydration, retained owner identity, accessibility, responsive behavior, and keyboard traversal. Live Chrome DevTools shows the expected menu/dialog tree and Lighthouse accessibility 100; only the existing development font-preload warnings remain. UI and patterns builds pass. The inventory-wide static and package gates are rerun at this boundary; WebKit and manual assistive-technology qualification remain release-wide work.

- Completed the M2 ActivityTimeline and Stepper line. Both use stable IDs and explicit completed/current/upcoming/error text plus distinct symbols, so color is supplementary. ActivityTimeline emits ordered article entries with paired machine/display time and retained busy semantics. Stepper emits a named navigation landmark, native progress, ordered steps, `aria-current`, and a strict status/link/action union that preserves Button versus Link semantics. Horizontal steps stack from their own container width, and compact/RTL layouts stay bounded. Two focused SSR/validation cases and four Chromium cases pass, covering native keyboard activation, non-color state output, narrow layout, long content, real delayed refresh with twelve intermediate frames, retained list/control nodes and focus, delayed hydration, and axe. The refresh test exposed a dynamic renderer that replaced a focused Button when only its record changed; stable branch rendering fixes that root cause. Live Chrome DevTools reports Lighthouse accessibility 100 and the expected navigation/progress tree. UI build and the broader static/package gates are rerun at the inventory boundary; WebKit and manual assistive-technology qualification remain release-wide work.

- Completed the M2 FileDropzone line. The component keeps a native file input and visible picker action as the primary no-script/keyboard/touch path, adds depth-safe drag/drop with live announcements, and applies MIME/extension, byte-size, total-count, and app-supplied validation identically to both paths. It delivers browser File objects but owns no transport; apps provide stable serializable upload items, visible status, progress, retry, removal, and authorization behavior. ID-keyed rows retain their nodes across real multipart upload progress, failure, and retry. Six Chromium cases and two focused SSR/configuration cases pass for picker/drop parity, type/size/count rejection, app-owned transport, stable refresh identity, read-only behavior, delayed hydration adoption of an early native file selection, axe, and descriptive per-file actions. Live Chrome DevTools reports Lighthouse accessibility 100 and no component console errors. UI build and the broader static/package gates are rerun at the inventory boundary; WebKit, large-file behavior, and manual assistive-technology qualification remain release-wide work.

- Completed the M2 SegmentedControl and TagInput lines. SegmentedControl uses one native radio group with controlled/uncontrolled ownership, roving Arrow/Home/End focus, disabled-option skipping, RTL geometry, compact sizing, bounded single-line scrolling or explicit wrapping, uncanceled reset semantics, and native pre-hydration selection adoption. TagInput owns ordered unique strings, repeated native form values, Enter/comma commit, Backspace/Delete removal, Alt-arrow/Home/End reorder with focus retention, long-token containment, and a retained native draft. Its optional sync/async validation combines AbortSignal with a monotonic revision, so pending, rejected, failed, aborted, and ignored-abort work cannot blank or overwrite accepted tags. Six focused unit/SSR cases and eleven combined RadioGroup/SegmentedControl/TagInput Chromium cases pass, including axe, RTL/read-only behavior, form projection, and delayed hydration identity. A live Chrome DevTools review reports a complete tag list/input accessibility tree, no component console errors, and Lighthouse accessibility 100. UI build, lint, workspace/Loupe typechecks, and the 167-component/171-example manifest pass. WebKit and manual assistive-technology qualification remain release-wide work.

- Completed the M2 date, time, and timezone package slice. `@gemologic/sheen-date` now exports Sheen-owned frozen `CalendarDate`, `Time`, `DateRange`, `TimeZone`, `DateTime`, and `WallDateTime` values plus strict parsing, validation, ISO form projection, locale/calendar formatting, instant/wall zone policies, and fail-closed DST ambiguity/nonexistence resolution. Calendar, DateField, DatePicker, DateRangePicker, TimeField, TimePicker, TimeZoneSelect, and DateTimePicker have exhaustive metadata and compiling demos. Scoped portals, responsive phone sheets, RTL machine behavior, bounds/unavailable dates, incomplete-range continuity, explicit DST choices, and real delayed option refresh retain server markup, nodes, focus, query, and accepted form state. FilterBar accepts the date selector through an injected adapter while its built table consumer retains no date implementation. Copied-package consumers measure 5.6/30.9/55.3/99.6KB gzip for core/field/picker/date-time fixtures under 6.2/34/61/110KB gates and expose no Ark UI or Adobe types. Fourteen focused unit/SSR cases and thirteen combined Combobox/date Chromium cases pass. The five-run production date benchmark measured about 16.8ms p99 with zero Long Tasks, frames over 50ms, unexpected layout shift, or owner/query loss across opening, keyboard acceptance, filtering, a real 350ms retained refresh, and DST resolution. The committed baseline is local bootstrap evidence; hosted Ubuntu, WebKit, and manual assistive-technology qualification remain separate release proof.

- Completed the M2 AdminApp and application-readiness qualification. The dedicated `@gemologic/sheen-patterns/admin` entry now owns a compact dark-default application baseline over AppShell with standard, workspace, horizontal, and inspector presets; twelve semantic chrome zones; validated placement overrides; independent chrome/navigation/action appearance axes; scoped theme, accent, toast, confirmation, notification, account, workspace, command, and details services; and a polished integrated DataTable starter. Loupe exposes all 30 legal zone-to-target combinations plus six themes, twelve accents, direction, density-safe appearance, and paged/continuous table behavior through URL-backed controls without remounting the shell. Sixteen Chromium cases pass, covering every preset and supported placement, delayed hydration owner reuse, server-resolved collapsed geometry, retained refresh content/focus/draft/scroll, authorization clearing, scoped portals, responsive docked/Sheet details, phone RTL cards and drawer behavior, all meaningful route states, open-overlay axe checks, keyboard traversal, forced colors, reduced motion, target size, long content, and 200%/400% reflow equivalents. The calibrated five-run production benchmark passes sidebar collapse, preset change, command opening, details dock/Sheet transitions, and retained refresh at about 16.8ms p99 with zero Long Tasks, frames over 50ms, unexpected shell shift, or remounts; intentional toast enter movement is recorded separately. Isolated consumers measure base AppShell at 32.4KB gzip and the complete Admin entry at 76.8KB under its 80KB gate while retaining no table, chart, date, or Composer code. Patterns/Loupe builds, workspace typechecks, lint, 11 focused Admin/baseline unit cases, DataTable compact presentation, package consumers, and the 157-component/161-example manifest pass. Local Chromium evidence is not WebKit, hosted-runner, manual NVDA/Firefox, or VoiceOver/Safari release proof.

- Completed the M2 production profiling and expanded DataTable benchmark lines. The production profile separated the Sheen client pipeline from browser reconciliation and counted row-ID, search-projection, accessor, and complex-cell fan-out. Before replacement, the representative 100,000-row integrated compact fixture mounted in roughly 223–235ms and multi-sorted in 319–336ms while a direct two-key sort was only 35–41ms; the duplicate `@tanstack/solid-table` core row model allocated wrappers and caches for every accepted row. DataTable now owns one validated source identity index and lazily realizes only viewport display rows while TanStack Virtual remains the viewport engine. No public table/column API or worker-only contract changed. The v2 five-run reference measured 110.5ms mount, 101.8ms multi-sort interaction, 62.2ms ranked-search interaction, 27.2ms filter, and 62.2ms same-ID refresh; an independent repeat measured 113.8/109.1/61.7/26.7/64.6ms and passed all normalized 10-percent gates. Both runs reported zero blank scroll frames, frames over 50ms, Long Tasks, or trace loss; the repeat scroll CDP p99 was 18.676ms. Every refresh run retained the accepted row node, focus, scroll, and updated value. Profiling also exposed a stale non-tree cell after same-ID replacement; returning its cell content through a reactive JSX boundary fixes the root cause. The package DataTable build is 22.94KB gzip, the Loupe production chunk fell from about 68.9KB to 48.47KB gzip, and copied built/source consumers prove no Solid Table runtime is retained. Twenty-two focused processing/fixture/baseline cases, 42 DataTable/selection cases, and the complete 90-case Chromium table regression pass with table/Loupe builds, workspace typechecks, changed-file lint, consumer/tree-shaking gates, and a Chrome DevTools production review showing 29 realized rows for 100,000 accessible rows with no console warnings or errors. The first repeat timed out only because a preview process retained an obsolete SSR asset manifest after a rebuild; restarting that dedicated preview made every referenced module return JavaScript and the unchanged gate passed in 29.9s. Hosted-runner and WebKit performance remain separate release proof.

- Re-measured the isolated full `@gemologic/sheen` public graph after the Combobox inventory landed. It is 94,054 bytes gzip, failing both the 60,000-byte release gate and the old 84,000-byte provisional ceiling. The retained-module report identifies Kobalte core and floating/presence machinery as the dominant graph, followed by Combobox and Select. The previously proposed 88,000-byte provisional ceiling is therefore obsolete before approval and must not be applied. Meeting the release target needs a package-boundary or replacement-implementation decision; changing the arithmetic or merely raising the ceiling would not complete the TODO.

- Qualified the existing list/detail, settings, form-heavy, and reading gallery compositions with seven real Chromium cases. Each composition exposes its intended native landmarks and application interaction, retains the exact accepted content and draft input owners through twenty sampled refresh frames, keeps the focused draft and never swaps in a skeleton, and accepts the next revision atomically. Permission loss removes accepted reading content before recovery, while a queued pre-hydration refresh preserves the server content and typed margin note. Lint and workspace/Loupe typechecks pass. The gallery checklist remains open only for the CommandPalette overlays, which depend on the pending palette implementation decision, plus broader cross-engine/matrix qualification.

- Added one combined hydration/refresh regression that starts from a stored light/slate/rose preference on the dark-default app, blocks network scripts, fonts, and the refresh response independently, types and activates refresh before hydration, then samples every animation frame. Chromium passes with the same input and region nodes retained, accepted content visible until atomic replacement, the draft preserved, and zero sampled dark-theme or blank-content frames. The test is enrolled in the WebKit gate, but local WebKit still cannot launch because its downloaded WPE wrapper cannot load `libatk-1.0.so.0` outside the unrealized Nix shell. No Nix realization or host mutation was performed, so the cross-engine checklist remains open.

- Completed the M2 Combobox and MultiCombobox line. Both wrappers validate stable option IDs/labels, separate committed selection from editable query text, support controlled and uncontrolled state plus native forms, and keep selected values resolvable across externally filtered subsets. App-owned remote search uses `filter={false}`, `pending`, retained non-selectable accepted results, independent transport errors, and retry. A real delayed HTTP fixture proves exact option-node retention for twenty pending frames, atomic result acceptance, failure/retry, multi-value FormData, Backspace/tag removal, scoped light/RTL portals, and input-node/draft reuse after delayed hydration. The browser work exposed and fixed a pointer-up race where input blur reordered a filtered list, SSR labels that depended on mount effects, and Kobalte's reactive hidden multi-select submitting only one value. Three SSR/validation cases, five Chromium cases, and one reviewed dark visual baseline pass with the UI build, lint, workspace/Loupe typechecks, and 144-component/148-example manifest validation. WebKit and the release-wide accessibility matrix remain open.

- Reconciled the M4 live-token editing line with its existing implementation and current browser evidence. Loupe owns one scope-local style element appended after theme CSS, explicit invalidation coalesces twenty same-frame writes into one token publication, and the existing canvas is repainted without replacement or a blank sampled frame. Five focused Chromium cases pass for scope probe lifecycle, inherited theme changes, last-write editor overrides, dark delayed hydration, and stored light first paint. The broader base-theme/import/export/persistence editor remains its own unchecked task.

- Completed the M1 generated Loupe component-pages line. The validated manifest currently drives 142 indexed pages with live scalar playground controls, generated JSX output, documented and authored variant matrices, package/source/token anatomy, exhaustive authored props, and accessibility/guidance panels. The inventory browser assertion now derives its expected count from the generated data instead of freezing an obsolete number. All four focused Chromium cases pass, including visual and delayed-hydration identity coverage. The first attempt did not launch Chromium because the calling shell omitted the already-realized Nix library path; rerunning through the existing `.direnv` profile passed without changing Nix state.

- Completed the M2 Tree line. The compound Tree/TreeItem API validates stable identities, keeps focus, selection, activation, and controlled expansion independent, and implements the WAI-ARIA visible-item arrow/Home/End model, sibling expansion, locale-explicit wrapping typeahead, and one roving Tab stop. Collapsed groups remain mounted, hidden, and inert; collapse returns descendant focus to the branch and removal recovers to the nearest visible item. Two SSR/validation cases and four Chromium cases pass, including a real delayed refresh that changes a label and adds a child with no blank frames or owner replacement, complete no-script dark markup, and hydration reuse of marked root/branch/collapsed nodes. UI build, lint, and workspace/Loupe typechecks pass. WebKit and the root bundle decision remain open.

- Completed the M2 NumberField, Slider, and RangeSlider line. NumberField keeps numeric app state while parsing/formatting locale digits, grouping, currency, percent, signs, and decimals; it exposes spinbutton keyboard controls plus raw native form submission and preserves the active draft/locale through reactive refresh. Slider uses one native range input; RangeSlider exposes an exact ordered tuple with constrained accessible per-thumb bounds. Three SSR/validation cases and five Chromium cases pass, including localized keyboard behavior, twenty-frame real background refresh continuity, complete no-script dark markup, hydration without node replacement, and a reviewed visual baseline. UI build, lint, and workspace/Loupe typechecks pass. The optimized full root probe is 84,872 bytes gzip before Tree, 872 bytes over the provisional 84,000-byte baseline; the actual under-60,000 release target and WebKit remain open.

- Completed the M3 palette-qualification line. The token compiler now gates adjacent categorical colors at CAM16-UCS 18 in normal vision and 7 after severity-one Machado protanopia, deuteranopia, and tritanopia transforms, reporting every failed simulation. A separate audit records weakest arbitrary pairs and achromatopsia previews without claiming color-only accessibility. Committed references pin the official CAM16 forward example, linear-light red transforms, policy thresholds, and bundled minima; all six themes qualify in both modes. Four focused palette cases plus the existing color/compiler suites pass, along with lint and workspace/Loupe typechecks.

- Completed the M3 accessible chart summary and view-as-table line. `ChartDataTable` uses a native pre-JavaScript disclosure, requires a narrative summary and explicit domain label, validates the accepted Float64 columnar snapshot, and mounts at most 50 rows per page by default with a hard configurable ceiling of 200. It formats dates/numbers from the effective scoped locale, defaults time to UTC, treats timezones as formatting-only, and renders `NaN` as an em dash plus localized missing-value text. Full chart props now require label/summary/xLabel and cannot disable the table alternative. The real delayed fixture retains the disclosure, table, visible row, Sparkline, and Stat roots through twenty pending frames; opening the server disclosure before scripts load survives delayed hydration with the exact open node. Nineteen focused chart unit/SSR cases and five Chromium cases pass, including localized gaps/pagination and a reviewed dark baseline. Installed consumers measure the table entry plus CSS at 5,621 bytes gzip, SVG plus CSS at 2,630 bytes, and Sparkline at 997 bytes, all without renderer retention. WebKit and renderer integration remain open.

- Completed the M3 dependency-free Sparkline and Stat/StatGroup line. Sparkline validates typed finite/NaN input, emits separate SVG path segments around gaps, retains deterministic dimensions for empty data and SSR, and uses only semantic chart tokens. Stat uses native term/definition markup and requires visible trend text whenever directional color/shape appears; StatGroup provides a scoped comparison surface. A real delayed endpoint proves accepted SVG/stat roots and exact paths stay mounted and nonblank through pending and rejected refreshes, while delayed hydration reuses complete dark server markup. Eight focused path/SSR cases and five Chromium cases pass, including a reviewed dark baseline. Installed-consumer gates measure Sparkline at 997 bytes gzip and the full SVG entry plus CSS at 2,407 bytes, with uPlot/d3 graph rejection. WebKit and the broader chart accessibility/table alternative remain open.

- Completed the M3 scoped canvas-token bridge line. `useThemeTokens` shares one hidden computed-style probe per effective provider or `ThemeScope`, resolves against the document or actual scope wrapper rather than an overlay portal, observes inherited theme-axis changes, batches reads on rAF, and publishes only changed requested values. Loupe-only invalidation lives at a dedicated package subpath. The fixed-size canvas fixture preserves opaque dark SSR fallback and exact node identity through delayed hydration; inherited mode changes stay nonblank over twenty sampled frames, twenty same-frame editor invalidations produce one redraw, and stored light preferences own first paint before delayed hydration. Two SSR cases and five Chromium cases pass, along with UI/charts builds and root/Loupe typechecks. The first editor test exposed portal attributes masking scope-local token overrides; separating overlay and token targets fixes the underlying ownership error. WebKit and renderer-level theme benchmarks remain open.

- Completed the SettingsLayout half of the grouped M2 SettingsLayout/SplitLayout line. SettingsLayout validates stable section IDs, keeps all app-owned controls mounted behind a desktop rail or CSS-only phone strip, registers dirty state with AppShell, and retains a permanent save bar through clean, pending, and failed states. Its real delayed endpoint proves captured-version save semantics: editing during a request remains dirty, failures retain the draft, and a following request succeeds without replacing layout, section, input, or save-bar nodes. Dirty navigation invokes the shell guard. A blocked-script case adopts an early input draft into dark server markup while retaining all marked nodes. Three SSR cases and six Chromium cases pass, including a reviewed dark baseline. Metadata now validates 133 components and 137 compiled examples. The grouped TODO remains open until SplitLayout and its Resizable dependency are complete; WebKit also remains open.

- Completed the M2 DataTablePage line. The pattern composes PageHeader, grouped app actions, independent query controls, controlled app-backed saved views, one table-owned selection/action model, regional cold/refresh/error/permission states, and an optional status footer without importing or obscuring DataTable ownership. Loupe exercises the real table and saved-view HTTP adapter in both continuous and numbered client modes. Background refresh keeps page, table, first-row identity, accepted text, and the exact scroll offset through twenty animation frames; authorization loss removes the table immediately. A blocked-script case proves dark server output, absence of unauthorized rows, and page-node reuse through hydration. Browser work exposed repeated JSX status-slot evaluation as a hydration mismatch, so the slot is now resolved once through Solid's owner-safe child helper. Three focused SSR cases and seven Chromium cases pass, including a reviewed dark baseline. The workspace build, lint, typechecks, 132-component/136-example manifest, and isolated consumers pass; the full UI probe is 83,891 bytes under the provisional 84,000-byte ceiling. WebKit and application-specific dogfood adapters remain open.

- Completed the M2 ListDetailLayout line. The pattern resolves stable local destinations only from the app router's accepted pathname/search/hash, keeps list and detail owners mounted, gives list links one roving Tab stop with Arrow/Home/End navigation, and restores per-entry detail scroll through AppShell. Below 768px CSS presents one retained pane at a time; activation focuses detail and Back restores the originating link. A real delayed API refresh retains article/pane identity, visible content, and scroll through twenty sampled frames. A blocked-script phone case proves dark server presentation, URL-selected detail, and reuse of all three marked layout nodes after hydration. Three unit/SSR cases and five Chromium cases pass, including a reviewed bounded dark baseline. The first runs exposed eager mutually exclusive JSX resolution, selectors aimed through ScrollArea's frame wrapper, tests interacting before observable hydration readiness, and click auto-scroll from offscreen refresh controls; those root causes are corrected. WebKit and release-wide assistive-technology qualification remain open.

- Completed the M2 DataTable benchmark line. A committed seeded 100,000-row/six-column fixture now exercises production mount, sort, filter, pointer resize, and a 10,000px/two-second continuous scroll across five fresh Chromium contexts. Same-run calibration and a versioned normalized baseline gate operation medians at 10 percent; browser/CDP traces separately gate p99 frames, stalls, Long Tasks, trace loss, and blank scrolling frames. The first local run exposed fixed rows being unnecessarily measured through the variable-height path; restricting element measurement to variable rows reduced the median filter interaction from 1,219.6ms to 40.4ms. Two corrected five-run executions pass; the final artifact records normalized render/sort/filter ratios of 5.6719/3.3563/0.9068, resize browser p99 16.8ms, scroll CDP p99 18.782ms, no frames over 50ms, no Long Tasks, no blank frames, and no trace loss. The pinned `ubuntu-24.04` CI job always uploads raw benchmark evidence; its first accepted artifact must deliberately replace the explicitly labeled local-bootstrap baseline, so hosted-runner performance remains unproven locally.

- Completed the M2 phone `mobileLayout` line. The opt-in below-768px card presentation is selected entirely by CSS over deterministic server/client markup. Continuous data receives a presentation-local 20-card default page with a validated 1–100 bound, while numbered tables reuse their one accepted page and existing Pagination state. Cards preserve shared sorting, selection, cell rendering/edit state, row activation, action callbacks, complete-view footer semantics, native long-press, and a visible overflow path. Positional roots survive delayed hydration and atomic server acceptance; refresh keeps all ten sampled cards opaque and populated, delays visible progress for 500ms, and disables stale-query actions. Two new SSR cases, five focused Chromium cases, and the 39-case combined DataTable regression suite pass, including local page reset, unobstructed selected-page navigation, desktop fallback, real HTTP refresh, twenty-frame hydration/refresh sampling, and a reviewed dark baseline. Build, lint, typecheck, the 130-component/134-example manifest, and isolated consumers pass; the full UI probe is 83,821 bytes under the 84,000-byte ceiling. WebKit remains a release boundary.

- Completed the M2 DataTable chrome and ContextMenu lines. Headers expose stable multi-sort order; sticky client footers aggregate the complete transformed view while server footers are explicitly page-scoped; one validated action model powers the selection overlay and per-row context menus with correct right-click selection and pending guards. Cold/no-result/empty/initial-error/retained-refresh/refresh-error states are distinct. Cold placeholders wait 200ms, retained rows stay opaque and blank-free, and visible refresh progress waits 500ms. `ContextMenu` preserves native `div`, `li`, and `tr` triggers and suppresses the platform event only when a replacement opens; pre-hydration and disabled/empty paths remain native. Table/filter English catalogs now live in the table package while preserving typed scoped `ThemeProvider` overrides, keeping the full UI graph at 83,823 bytes. Twenty-six focused unit/SSR cases, thirty-four combined DataTable Chromium cases, and seven final chrome/hydration cases pass; four dark baselines were reviewed. Build, lint, typecheck, the 130-component/134-example manifest, and isolated consumer checks pass. WebKit and application-owned production adapters remain release boundaries.

- Completed the M2 inline cell-editor line. Accessor columns can declare validated text, number, boolean, or select editors; resting cells remain focusable text and swap to native uncontrolled controls on focus, click, or Enter. Blur and Enter commit, Escape discards, and Tab follows native order. Per-cell controllers survive virtualization, preserve dirty drafts through refetch with visible stale markers, reject superseded ignored-abort completions, expose app-reported conflicts, and keep transport failures editable with localized toast retry. Authorization reset aborts work and discards drafts. Twenty-five focused column/controller/DataTable cases and seven real-HTTP Chromium cases pass, covering synchronous validation, delayed hydration click adoption, failure/retry, refetch continuity, conflicts, supersession, editor-type traversal, and reset. Full builds, lint, typechecks, isolated consumers, 129-component/133-example metadata, and the 83,906-byte full UI probe under the provisional 84,000-byte ceiling pass. WebKit and application-owned production commit adapters remain release/dogfood qualification boundaries.

- Completed the M2 DataTable export line. Client tables snapshot the complete accepted filtered/sorted in-memory view, visible accessor columns, and active explicit or inverted selection independently of pagination and virtualization. CSV quotes every value with CRLF records and neutralizes formula-leading strings, while JSON preserves supported scalar types. Server tables hide export without `onExport`, delegate immutable accepted state/selection plus AbortSignal without walking pages, retain the last successful Blob through failure, retry the captured request, and revoke it when `export.resetKey` crosses an authorization boundary. Thirty focused export/controller/selection/DataTable cases and 29 combined DataTable/FilterBar/export Chromium cases pass, including paginated whole-view downloads, hidden columns, selection narrowing, real delayed server success/failure/retry, 20 pending animation frames with zero blank table bodies, delayed hydration with one retained trigger/menu and zero provisional requests, and reviewed dark baseline updates limited to the new Export trigger. Full package and Loupe builds, 129-component/133-example metadata, isolated consumers, lint/typechecks, and the 83,761-byte full UI probe under the provisional 84,000-byte ceiling pass. WebKit and application-owned production export execution remain release/dogfood qualification boundaries.

- Completed the M2 FilterBar line. FilterBar is a controlled editor over the existing immutable filter AST, preserving nested boolean structure while reconciling chip DOM identity across validated copies. Searchable column selection and text, number, enum, empty, negation, and inclusive UTC date-range drafts publish only on Apply; self-excluding enum counts come from accepted complete-view/server facets. DataTable integrates declared filters into its query lifecycle, retains accepted rows through real delegated refresh, and adopts queued server-rendered filter triggers after hydration without duplicate layers or provisional requests. Twenty-six focused AST/state/SSR cases and 24 combined DataTable/FilterBar Chromium cases pass, including all typed editors, direct-negation edits, removal focus recovery, delayed real HTTP frame sampling with zero blank frames, delayed hydration, and reviewed dark baselines. Full builds, 129-component/133-example metadata, isolated consumers, lint/typechecks, and the 83,700-byte full UI probe under the provisional 84,000-byte ceiling pass. WebKit, the 60,000-byte release target, and domain-specific non-UTC calendar semantics remain explicit boundaries.

- Completed the M2 grouping and hierarchy line. Complete continuous client views group in first-seen order with configured count/sum/average/min/max aggregates; paginated server views must supply explicit contiguous page membership plus full-query counts and aggregates, which DataTable validates atomically instead of inferring from a page. Static and app-loaded hierarchies expose treegrid metadata and Left/Right navigation, abort per-node work on collapse/reset/disposal, reject late ignored-abort results, retain failed parents for retry, and clear loaded subtrees at authorization boundaries. Default-expanded remote nodes wait for hydration, and an early native expander click replays on the retained focused server button. Twenty-one focused state/SSR tests and four Chromium cases pass, including real HTTP loading/failure/retry, delegated groups across pages, delayed hydration, stale-response rejection, and reviewed dark baselines. WebKit and full release-wide assistive-technology qualification remain open.

- Completed the M2 DataTable selection line. The public component now owns native page and row checkboxes, explicit IDs retained across unloaded pages, immutable filter/exclusion all-matching payloads, range and modifier gestures, row Space, Shift+Arrow, and table-scoped Ctrl/Cmd+A. Accepted filter or search changes clear selection, fuzzy search suppresses unsafe all-matching actions, and `resetKey` clears selection plus eligibility at account/permission boundaries. Selection remains presentation-local through sort, page, layout, and pending server requests; apps retain authorization and bulk-execution ownership. Nineteen focused state/SSR cases and eighteen combined DataTable/selection Chromium cases pass, including cross-page server retention and pre-hydration native checkbox adoption without DOM or focus replacement. WebKit and remote bulk-action execution remain release/application qualification boundaries.

- Completed the M2 column sizing/layout line. DataTable owns fixed/fill/content tracks, bounded pointer and keyboard resize, realized-cell auto-fit, reset, stable drag/keyboard reorder, visibility, and logical start/end pinning. Pointer resize writes one CSS variable per animation frame and commits only on release; layout-only changes never dispatch server data requests, including while a request is pending. Stable keyed columns retain cell and focus identity across moves, RTL pinning stays fixed during horizontal scroll, and delayed hydration preserves the server resize node and focus. Eighteen focused state/SSR cases, twelve Chromium cases, a reviewed dark baseline, UI/table builds, lint/typechecks, metadata validation, isolated consumers, and the Loupe production build pass. The full UI probe is 83,281 bytes gzip beneath the 84,000-byte anti-regression ceiling; WebKit and calibrated resize frame-stall qualification remain release-wide work.

- Completed the M2 Avatar/AvatarGroup and Progress/Meter lines. Avatar keeps deterministic fallback content mounted beneath loading images, detects failures that occurred before hydration, retains its root through source changes, and localizes bounded group overflow through scoped messages and number formatting. Progress and Meter use native elements with strict number-only APIs and reactive range validation. Three SSR tests, two Chromium cases, a reviewed dark baseline, UI build, lint/typechecks, and exhaustive validation at 128 components and 132 examples pass. The delayed fixtures prove server-node reuse and sample every animation frame of a real image refresh without an empty Avatar. Replacing the Checkbox, Switch, and RadioGroup internals with native inputs while preserving their browser contracts brought the independent full-import probe below its 84,000-byte anti-regression ceiling; the 60,000-byte release target remains open.

- Completed the M2 general Drawer/Sheet line by reusing ModalFrame's scope-owned portal, modal shortcut scope, top-layer dismissal, focus trap, and trigger restoration. Logical start/end geometry follows LTR/RTL, occupies the dynamic viewport height, and uses the specified bounded slide/opacity motion with reduced-motion token collapse. One SSR test, two Chromium cases including delayed-script trigger reuse, UI build, lint/typechecks, 124-component/128-example metadata validation, and isolated consumers pass. WebKit runtime remains part of the release-wide gate.

- Completed the M2 fixed/variable virtualization line. DataTable defaults to density-token fixed rows, invalidates estimates across live 28/34/42px density changes, measures variable content only after hydration, and batches ResizeObserver delivery through animation frames. ID-keyed realized-row wrappers plus same-query inset restoration preserve the visible pixel anchor, DOM marker, and focus when a row is prepended above the viewport. Optional row activation provides roving focus; End/Home realize distant rows before focus and Enter activation. Four SSR tests and eight Chromium cases pass, including fixed/variable frame-sampled scrolling with zero blank body frames, delayed-script hydration, paging, failure/retry, and atomic server acceptance.

- Completed the M1 router task. The framework-neutral adapter now has a packaged Solid Router entry, dirty-navigation blocking, history-entry identity, two-axis desktop/phone pane restoration, and accepted-route title ownership through the app-provided `@solidjs/meta` provider. Three new Chromium title cases prove one escaped SSR title, reactive back/forward changes without shell remount, blocked destinations retaining the current title, fallback restoration on disposal, and twelve stable post-hydration title frames while preserving the server shell node. Six AppShell SSR tests, the patterns build, metadata validation, lint/typechecks, and isolated router consumer resolution pass. The adapter remains intentionally separate from app copy and loader ownership.

- Completed the M1 Button-family and remaining shortcut-composition lines. A typed `ShortcutAction` now gives Button, Tooltip, and IconButton one registration/activation/hint contract: the shared registry invokes the native button, platform-formatted tooltip text comes from the accepted binding, and disabled/loading controls cannot activate through the shortcut. Display-only strings remain explicit. Together with the existing scoped registry, sequences, pending status, generated question-mark sheet, and AppShell `?`, `mod+/`, and development-only `mod+shift+d` bindings, this closes the M1 shortcut assembly without registering `mod+k`. Two Button SSR cases, nineteen related registry/control unit cases, sixteen combined Button/IconButton/Tooltip Chromium cases, UI build, 77-component/81-example metadata validation, lint, and workspace/Loupe typechecks pass. The reviewed tooltip snapshot changed only from literal `mod+s` to the host-correct `Ctrl+S`. The separate user-facing character-shortcut/remap and actual speech-input qualification task remains open.

- Added isolated package-entry budget fixtures and selective token CSS exports. Individual theme files no longer duplicate all accents; `accents/<name>.css` is independently consumable. Current production gzip evidence is 6,529 bytes for core plus one theme/accent, 12,205 bytes for core plus every theme/accent, and 1,247 bytes for a Button-only consumer. The first Button probe was 24.2 KB because Tailwind Variants/Merge supplied a runtime utility parser; replacing it with a small typed sheen recipe and honest `clsx` flattening removed that cost while cascade-layer browser tests preserve caller precedence. Native Checkbox, Switch, RadioGroup, and Separator internals preserve platform form behavior while the full UI graph is 83,821 bytes. The 60,000-byte release target and explicit 84,000-byte anti-regression ceiling remain distinct, so the package-budget checklist line remains open.

- Completed the four remaining core token/theme checklist lines. Paper, Vellum, Slate, and Graphite now define complete and visually distinct light/dark surface, text, line, control-boundary, and focus systems instead of inheriting nearly all of the Obsidian palette; Vellum also carries its reading type and spacing scale. Contrast explicitly owns untinted surfaces and strong line roles in both modes. Compiler tests prove distinct full palette signatures, dark defaults, semantic/type scales, tier-three fallbacks, all-diagnostic validation, compositing, and all 144 theme/mode/accent combinations. Seventeen focused compiler cases pass, the token package rebuild emits all six CSS entries, the rendered Chromium matrix passes 4,032 buttons across every accent, surface, tone, theme, and mode, and the eight M0 theme/hydration cases pass. The M0 comparison selected jade as the recorded default while retaining six themes and twelve independent picker choices.

- Completed the M2 pagination and public server-dispatch lines. `DataTable` now runs bounded continuous client data and client/server numbered pages through one accepted-state contract, virtualizes both presentations, clamps client pages atomically, and never walks hidden server pages. Server requests retain accepted row DOM through pending and failure, expose retry, reject stale controller results, and accept query plus rows atomically. Authoritative `initialResult` skips the mount request and delayed-script hydration reuses the server row node. Eight pagination tests, four client-state tests, nine real HTTP request tests, four DataTable SSR tests, and eight Chromium cases pass. Isolated built and Solid-source consumers also verify the published component/CSS, while the new `@gemologic/sheen-table/core` entry keeps dependency-free state helpers usable without linking browser components.

- The underlying server controller still provides the stronger ownership guarantees behind DataTable: cloned requests, AbortSignal, monotonic bigint tokens, atomic accepted query/results, ignored-abort suppression, bounded clamp refetches, complete-result validation, and authorization-safe clear even under abort-listener reentrancy. Existing real loopback HTTP and repeated browser fixtures cover late out-of-order arrivals, moving totals, clear/dispose, retained editors/focus/selection, and previous-query action guards beyond the public component cases above.

- Completed the M2 client-processing line. Versioned filters validate typed text/number/date/enum predicates, nested all/any groups and negation with bounded depth/size; compiled evaluation is locale-explicit and non-coercing; sorting is stable and multi-key; facets count the full transformed result; ranked search uses deterministic exact/prefix/substring/subsequence scores; and the trailing controller preserves accepted identity across pending, failure, supersession, clear, and disposal. `createClientView` composes search, filters, full-result facets, sort, and paginated or continuous output in that order, and the public DataTable now consumes this pipeline with interactive single/shift multi-sort. Forty-nine deterministic unit cases and the existing repeated Chromium search fixtures cover real debounce, rapid supersession, complete-view facet counts, page reset/clamping, retained drafts/rows, and SSR hydration without evaluation. FilterBar/search chrome remains its separate checklist item.

- Completed the M1 mobile-drawer and SidebarNav line. AppShell projects one retained sidebar owner between desktop aside and scoped phone Dialog, keeps desktop and phone accepted state independent, restores focus across breakpoint and dismissal boundaries, and preserves two-axis pane offsets. SidebarNav validates stable identities, resolves one most-specific route, expands active ancestors, preserves manual collapse state on same-route refresh, retains keyed links/groups, and recovers focus after removal or rail collapse. Eight targeted SSR/unit cases and 28 combined Chromium cases pass, including reviewed dark/light RTL visuals, delayed hydration, controlled proposal rejection, nested Select projection, retained drafts/nodes/scroll, empty navigation, reorder/removal, and active-route traversal. A full parallel run exposed an Escape sent before the exiting drawer unmounted; the test now waits for observable layer disposal, and the six LTR/RTL handoff repetitions plus the repeated 28-case run pass.

- Completed the M1 structural shell line. The native ScrollArea owns accessible keyboard scrolling and measured overlay tracks without reserving a gutter; AppShell constrains header/sidebar/content/status tracks at desktop widths, locks competing document scroll, and deliberately returns primary content to document flow below 768px while suppressing pane overlays. Seven targeted SSR/unit cases and 14 combined Chromium cases pass, covering server-first constrained layout, hydration retention, independent panes, real phone wheel scrolling, forced colors, pointer capture/disposal, scaled and RTL dragging, resizing, focus geometry, refresh continuity, and no document overflow. Responsive drawer/navigation behavior remains its own checklist line.

- Completed the M1 shortcut-registry ownership line. Registrations are owner-scoped and reactive; nested focus and modal scopes shadow predictably; same-scope collisions throw in development and warn with newest-wins restoration in production; browser-reserved chords, editing, AltGraph, composition, repeat, and widget keyboard ownership are fail-safe; pending sequences and document listeners clean up on focus, removal, scope changes, and disposal. Nineteen targeted unit/SSR cases and 26 combined Chromium cases pass, including automatic platform hydration, live binding replacement, real async rejection, input/contenteditable suppression, Select/menu typeahead, nested modals, pane remounts, and provider disposal. Generated help, tooltip association, baseline shell bindings, and the consumer-facing character-shortcut preference remain separate checklist lines.

- Completed the M1 application-state hooks line. Owner-scoped dirty registrations aggregate inside AppShell, navigation blocking retries only the exact accepted attempt, beforeunload exists only while dirty, and browser focus/online hooks expose unknown SSR state without claiming backend health. Twelve targeted unit/SSR cases and 14 combined real Chromium cases pass, including delayed hydration, native unload prompts, multiple editors, owner disposal, unrelated router blockers, history-forward retry, responsive drawer projection, actual browser offline transitions, and native tab focus. The packaged Solid router adapter and title synchronization remain in the separate router task.

- Completed the M1 regional-state pattern group: PageHeader, measured whole-group Toolbar overflow, app-driven StatusBar connection/task presentation, regional ErrorState retry ownership, and delayed cold/refresh LoadingState. Seventeen targeted SSR/unit cases and 28 combined Chromium cases pass, covering keyboard behavior, real request failures, disposal, resize and narrow layouts, delayed hydration, retained DOM/drafts/focus, frame-sampled refresh continuity, and reviewed dark/light RTL baselines. A PageHeader case exposed a lazy-hydration race in its own setup; it now waits for an observable direction update before sending non-replayable arrow input, and all nine PageHeader executions pass across three repetitions. Cross-engine and full milestone-wide contrast qualification remain in the separate 0.1.0 gate.

- Completed the M2 URL/saved-view task. A router-structural URL owner now validates duplicate/malformed state, preserves unrelated query/hash state, supports replace or push semantics, and restores through browser history. A saved-view controller owns real adapter list/save/delete, versioned restore, exact failure retry, incompatible-state reporting, abort tokens, clear, and disposal without localStorage. Five real HTTP tests, eight actual-router Chromium executions, six saved-view Chromium executions, production Loupe build, and static checks pass. Generic DataTable toolbar placement and application-specific migrations remain separate work.

- Completed the M2 column/state definition task. `defineColumns` now returns an opaque branded value, validates and normalizes sheen-only accessor/display definitions, preserves logical pinning/faceted options, derives deterministic schema metadata, and contains `__unsafe_tanstack` in one copied typed record. `createRowIdentity` rejects unstable, duplicate, empty, and sparse identities while preserving row objects. Three column and two row-ID tests, table build, lint/typechecks, and an isolated built consumer pass. TanStack runtime translation, its lint warning, column interaction UI, and performance remain separately open.

- Added framework-neutral cell-edit ownership with scalar validation, uncontrolled drafts, monotonic commit IDs, transport retry, changed-draft retry rejection, explicit version conflicts, stale-refetch preservation, supersession, discard, and disposal. Six real HTTP cases pass. A real Loupe editor and endpoint now pass ten Chromium executions for delayed hydration, invalid state, transport rollback with toast retry, dirty refetch, persistent conflict, and ignored-abort supersession. Browser work exposed and fixed a programmatic focus-restoration loop. Generic DataTable editable-column integration, Tab traversal, multiple-cell ownership, and cross-engine behavior remain open, so the grouped inline-edit TODO is not checked.

- Completed the M1 refresh-continuity acceptance line across real delayed and erroring loading, table-request, and pane-restoration fixtures. Frame sampling proves no blank or skeleton refresh frames; stable markers and geometry prove retained content/editor identity, drafts, focus, text selection, and scroll anchors. The current combined run passed 19 of 20 cases and exposed a browser-consumed `Alt+r` fixture shortcut before any request occurred. Replacing it with an explicit request trigger made all four table cases pass three consecutive repetitions. This qualifies the shared refresh contract, not every future component or WebKit.

- Added server export ownership with optional adapter availability, immutable full-query/selection capture, duplicate-request guarding, retained successful artifacts, captured-request retry, cancellation tokens, and disposal. Five real HTTP tests pass, along with the table build, lint, typechecks, and isolated package consumer checks. A real delayed Loupe endpoint and six Chromium executions prove hidden adapter-less UI, hydration without dispatch, one-operation guarding, whole-view export despite pagination, retained link identity through failure/retry, exact CSV/JSON downloads, cancellation, and restart. Generic DataTable integration, cross-engine downloads, large-file behavior, and route-unmount URL cleanup remain open.

- Added real CSV/JSON Blob downloads to the selection fixture using native sheen Link download semantics. Six Chromium executions pass. Tests read actual downloaded files, verify filenames/CSV bytes/JSON row contents, cross-page selection and hidden-column exclusion, prepared-snapshot isolation, and replacement URL revocation. Lint/typechecks pass. Generic export UI, server gating, other engines, large-file behavior, and spreadsheet interoperability remain open.

- Added client selection resolution against the complete accepted view, preserving view order/row identity for explicit IDs and re-evaluating captured query filters plus exclusions. Five focused tests cover cross-page export, hidden columns, invalid IDs, and omitted deleted IDs. The real selection fixture prepares exact CSV output for explicit and inverted payloads. Actual browser downloads, server export gating, and full DataTable state/selection integration remain open.

- Added client export snapshots with visible-column ordering, scalar-only values, RFC-style CSV quoting/CRLF, JSON type preservation, formula-looking string/header prefixes, and no hidden-column accessor reads. Six focused tests cover serialization, snapshot/property safety, invalid/sparse inputs, and errors. Browser downloads, accepted selection/state integration, server export gating, and actual spreadsheet interoperability remain open; CSV defenses do not claim universal spreadsheet safety.

- Added real page/all-matching selection controls with immutable captured bulk payloads and filter-reset behavior. Browser work reproduced an ownership warning from event-time reads of a compiled checked getter in the shared Checkbox/Switch helper; caching the prop in an owned memo removes the warning. The regression plus checkbox/switch keyboard/reset/hydration cases pass twice, twelve Chromium executions; four unit/SSR tests, UI build, consumer checks, lint/typechecks pass. Virtualized selection/focus, remote full-query bulk execution, and generic DataTable integration remain open.

- Added explicit/inverted table selection ownership with internal Sets and detached immutable bulk payloads, loaded-row eligibility, retained IDs/exclusions across pages, inclusive ordered ranges, filter-change clearing, and account-boundary reset. Six focused cases cover atomic updates and no materialization of unloaded matches. Full-query search criteria must be represented before enabling inverted bulk actions; checkbox/keyboard UI, counts, and generic DataTable integration remain open. TanStack installation still awaits approval.

- Reproduced a sparse enum-schema validation failure: Array.every skipped absent options, so an invalid declaration passed parseFilter. Replaced it with iteration that validates every entry, including holes. Dependency research identified Solid Table 9.2.4 and Solid Virtual 3.13.37 as current compatible-range candidates; explicit installation approval is pending. See docs/table-dependencies.md for API/Store and runtime-proof boundaries.

- Added immutable full-table state changes for query updates, exact-permutation column order, visibility, width/reset, logical pinning, and three-state sort cycles. Multi-sort preserves priority and appends new keys; sorting resets pages while layout-only changes retain query/page state. Five focused tests and the table build pass. Rendering/keyboard controls, bounded column-definition sizing, focus restoration, and complete DataTable integration remain unfinished.

- Added strict versioned table-state serialization with query state and ordered column visibility/width/logical-pin settings. Parsing freezes copies and rejects malformed, incomplete, duplicate, unknown-column, schema-drift, unsupported-version, and oversized input before restoration. Five focused tests and the table build pass. The format owns no persistence, selection, or grouping state; saved-view/router lifecycles, column controls, and branded definitions remain unfinished.

- Added immutable validated client query-state parsing and shared transitions for search/filter/sort/page/page-size changes. Direct and indirect size changes reset page zero; continuous mode stays continuous. Loupe now uses the transitions and adopts clamped pagination only after accepted evaluation. Four focused tests, table build, and eight Chromium executions pass. Full versioned table/column state, saved views, generic DataTable ownership, and performance qualification remain open.

- Extended the real client-view fixture with enum filtering, descending sorting, accepted Pagination controls, full-result facet/total outputs, and continuous-mode switching. Four Chromium cases repeated twice pass, including filter/search page resets and full-result facet counts on a one-row page alongside draft/focus/hydration regressions. Full FilterBar, generic versioned state ownership, virtual scrolling, and performance qualification remain unfinished.

- Added the complete client processing composition: search, filter, full-result facets, stable explicit sorting, then pagination. Results expose both complete view and visible page with effective clamped pagination, preserving row objects and input state. Four focused tests, table build, public-package consumer checks, lint/typechecks, and six Chromium executions pass. Loupe's search fixture now uses this pipeline without losing hydration/refresh continuity. Branded columns, full versioned state/transitions, complete control UI, virtualization, and calibrated performance remain open.

- Added accepted initial search results without automatic evaluation and a real Loupe SearchInput/table integration. Eight controller tests and three Chromium cases repeated twice pass. Browser evidence covers animation-frame row/editor/focus/selection continuity, final-query-only evaluation under rapid native input events, previous-query action guards, and delayed hydration retaining a typed draft with zero initial evaluations. Table build and lint/typechecks pass. The fixture is three rows, not full DataTable, virtual scrolling, dynamic SSR-loader serialization, or calibrated performance qualification.

- Added a framework-neutral trailing search controller with a validated 120ms default delay, settled superseded/cleared/disposed requests, retained accepted query/results during pending/failure, retry including empty queries, and reentrant-result suppression. Seven real-timer tests and the table build pass. The evaluator reads the current complete dataset at execution time and remains synchronous. Reactive browser presentation, SSR initialization, complete table integration, and performance qualification remain unfinished.

- Added dependency-free ranked client search over explicitly configured columns: exact/prefix/substring/subsequence scores, AND semantics across distinct terms, stable ties, explicit-locale NFC/lowercase normalization, finite numeric search, and no arbitrary value coercion. Five focused tests and the table build pass. Search preserves row identity and documents explicit sorting precedence. Trailing debounce/cancellation, retained-result browser integration, complete DataTable state, and calibrated 100k-row performance remain unfinished.

- Added immutable client enum facet counts over the complete filtered result before pagination, preserving declared option order and zero counts, tracking null/undefined separately, and rejecting unknown values with row/column diagnostics. Map accumulation handles special property strings safely. Five focused tests and the table build pass. Counts deliberately describe current results without rewriting nested filter predicates. FilterBar, accepted server-facet validation, full DataTable integration, and performance qualification remain unfinished.

- Added validated sheen-owned sort-state entries and stable multi-column client sorting before pagination. Explicit supported-locale collation provides natural text ordering; numeric/date values are not coerced, missing/type-invalid values remain last in both directions, ties retain input order, and accessors run once per active key per row. Six focused tests and the table build pass. Documentation separates client stability from server pagination tiebreakers and SSR collation-data differences. Header interaction, facets/fuzzy search, full state/column integration, and performance gates remain unfinished.

- Added compiled client filter evaluation with explicit locale, NFC and locale-lowercase text matching, exact enum membership, finite numeric/date comparisons, inclusive ranges, and documented two-valued missing-value semantics. Compilation validates/copies the AST once; groups short-circuit and filtered rows retain identity/order before pagination. Nine focused tests pass, including accessor errors and mutation isolation. Table build and isolated public-package evaluator type/runtime checks pass. Sorting, facets, fuzzy search, branded columns, full table state/UI integration, and performance qualification remain open; no grouped TODO is marked complete.

- Added the schema-validated, versioned filter AST with logical groups/negation, text/numeric/date/enum predicates, immutable normalization, strict unknown-field/schema diagnostics, and bounded parsing/serialization. Seven tests cover URL/Unicode round trips and malformed/oversized/cyclic/sparse input. Built table consumer checks exercise public serializer exports; table build, lint/typechecks, manifest validation, and existing consumer checks pass. Predicate evaluation, locale/missing-value semantics, sorting/facets/fuzzy search, branded columns, and full table state/UI integration remain unfinished.

- Added validated initial accepted results to the server table controller, enabling SSR without an automatic on-mount fetch. Initial incomplete/out-of-range data is rejected; empty pages normalize and row identity is preserved. The fixture now renders deterministic initial rows server-side. A delayed-JS browser case proves retained server/editor nodes, pre-hydration draft/focus/selection, and zero duplicate data requests. All four browser cases pass twice; seventeen table tests, isolated initial-state consumer checks, lint/typechecks, table build, and Loupe production build pass. Dynamic SSR-loader serialization and complete DataTable integration remain unfinished.

- Added a real Loupe integration fixture for the built table request controller with a bounded delayed/erroring HTTP endpoint and ID-keyed row/editor rendering. Three Chromium cases pass twice each, including animation-frame samples for retained rows/editor/focus/selection, previous-query labeling and disabled actions, ignored-abort late results, and cleared results remaining empty after controller completion. Lint/typechecks pass. Loupe now depends on the existing table workspace, with no new third-party dependency. This is a three-row integration harness, not virtualized DataTable or full hydration/edit-conflict/scroll qualification.

- Reproduced and fixed an atomic-clear bug in table request ownership: a synchronous abort listener could restart old work and publish it after clear. Clear now publishes empty state and blocks synchronous restarts, including nested clears. A second real HTTP test verifies ordinary supersession preserves ownership of a newer abort-listener request so it remains abortable. Fifteen table tests, table build, lint/typechecks, and existing isolated consumer checks pass. Future producer authorization and full reactive/browser DataTable integration remain app/integration obligations.

- Added framework-neutral server table request ownership with abort plus monotonic response tokens, coherent accepted state/results, retained rows during pending/failure, explicit retry/clear/dispose, response completeness checks, and four-attempt bounded clamping. Five real loopback-HTTP cases cover ignored abort, late success/error suppression, failures, cleanup, clamping, and incomplete data; thirteen table tests pass overall. Table build, lint/typechecks, manifest validation, and existing isolated consumer checks pass. Reactive DataTable/browser integration and the complete request-dispatch TODO remain unfinished.

- Established the sheen-table workspace with dependency-free pagination state helpers, strict input validation, client continuous/paginated slicing, resets, safe clamping, and server response assessment. Incomplete continuous results and inconsistent page lengths fail; positive-total deletion cases return a clamped refetch decision, while empty results settle without a loop. Eight unit tests, table build, isolated public-package type/runtime checks, lint/typechecks, and manifest validation pass. Added the package to root build order and the workspace lockfile without external dependencies. DataTable, request dispatch, virtualization, processing, and Pagination integration remain unfinished.

- Reproduced and fixed Pagination focus falling to the document when an external accepted-result update removes the focused numbered button. It now recovers to the accepted page, or the first control for empty results, without requesting data or stealing outside focus. The focused-update regression failed before the fix; all four Chromium cases pass twice afterward. Four SSR tests, UI build, lint/typechecks, and manifest validation pass (77/80). Broader visual/accessibility/cross-engine and DataTable integration work remains open.

- Implemented public Pagination with controlled accepted state, bounded numbered controls, first/previous/next/last requests, pending activation guards, retained keyboard focus, and localized live summaries. Added metadata/demo, a real delayed/rejecting Loupe fixture, and four SSR tests. Three Chromium cases pass twice each for accepted-versus-requested state, retry, large/empty counts, Arabic RTL formatting, and hydration replay. UI build, lint/typechecks, and manifest validation pass at 77 components / 80 examples. Visual/assistive-technology/cross-engine qualification and DataTable integration remain unfinished, so the Pagination TODO stays open.

- Added test:consumer-browser for actual isolated production client execution from both copied built-JS and Solid-source package entries. Full client bundles include Solid and are served by a real loopback HTTP server, without Loupe or workspace aliases. Chromium verifies reactive Button Enter/Space, retained focus/identity, loading/native-click suppression, component CSS, and Link semantics. Both entry paths pass on the final run; consumer types/builds/Node SSR and lint/typechecks also pass. Added CI execution after Chromium installation and failure trace uploads. Isolated hydration, other engines, fresh registry installs, and size budgets remain open.

- Extended isolated UI consumer qualification to the Solid source export condition and real Node SSR execution. The source client build must retain copied source Button/Link, exclude UI dist mixing and headless/chart/table modules, and emit public CSS. A separate Solid SSR build executes without document and renders native loading Button plus button-styled Link through package exports. All consumer checks, lint, and typechecks pass. No production code or dependencies changed. Fresh registry installation, client-bundle browser execution, full inventory consumers, and size budgets remain open.

- Extended check:consumers with an outside-workspace UI fixture that imports copied package files by public package name. Strict consumer types exercise the shared APIs and negative native-prop cases. A real Vite build retains copied dist Button/Link exports, resolves public component/token CSS, and excludes headless/table/chart/source-TSX code from retained output with Solid external. Both consumer checks, lint, and typechecks pass. The fixture reuses installed dependencies; fresh registry installs, source-condition builds, compiled-entry browser execution, gzip budgets, and full release qualification remain open.

- Added the public sheen-owned `buttonVariants` recipe and `ButtonVariantProps`, shared by Button, button-styled Link, and the built-in Dialog trigger. Styling no longer requires consumers to recreate internal data attributes; the component layer remains utility-overridable. The initial Tailwind Variants/Tailwind Merge implementation was later removed after the package budget gate showed a Button-only consumer retained 24.2 KB gzipped. `cn` now honestly flattens conditional values while cascade layers give consumer utilities precedence, avoiding a runtime utility parser. Added the centralized type-only SheenPolymorphicProps export and built-declaration positive/negative consumer checks. Four targeted unit/SSR tests, UI build, lint/typechecks, manifest validation (76/79), and 28 Chromium executions pass. Native composition matches wrapped styles, caller utilities win, and Dialog's outline/focus behavior is preserved. Shortcut association and the full grouped TODO remain open.

- Implemented the missing public Link with required native href and text/button appearances, shared focus styling, metadata/demo, and a real Loupe route. Two SSR tests and four Chromium cases repeated twice pass, including JavaScript-disabled navigation, Enter versus Space, new tabs, modified clicks, downloads, reactive anchor identity, and hydration reuse. UI build, lint/typechecks, and exhaustive manifest validation pass at 76 components / 79 examples. WebKit discovery is 57 tests in fourteen files, with runtime still unverified. Shared variants/polymorphism, visual qualification, and the grouped button-family TODO remain open.

- Qualified IconButton's four sizes across all three densities and LTR/RTL using a reactive ThemeScope fixture. Focused dark/light theme changes preserve button identity and focus, update computed foreground, and propagate the selected theme into the still-open tooltip portal. All five native Chromium cases pass twice (ten executions); lint/typechecks pass. WebKit discovery now reports 53 tests in thirteen files, not runtime proof. Full visual matrices and the remaining button-family API work stay open.

- Qualified IconButton native form behavior with a real form fixture. Submitter name/value reaches FormData, keyboard reset restores the input, and loading suppresses native click and implicit Enter submission. All three IconButton Chromium cases pass twice each; lint/typechecks pass. No production fix was needed. Added the cases to WebKit selection, without claiming runtime or hosted-CI proof. Button-family API completion and visual/density/RTL qualification remain open.

- Implemented the missing M1 IconButton by composing the existing scoped Tooltip/native Button contract. Required label supplies the accessible name and tooltip, icon children are decorative, control sizes are square, and direct oversized SVG/image children are bounded by the icon token. Added public exports, metadata/demo, a Loupe fixture, two SSR tests, and two native cases passing twice each for loading suppression, square sizing, oversized SVG containment, live renaming, keyboard actions, and delayed hydration. UI build, lint/typechecks, and exhaustive manifest/example validation pass at 75 components and 77 examples. No dependency was added. The semantic icon registry, full button-family seam, visual matrices, and cross-engine qualification remain open.

- Extended ButtonGroup qualification to LTR/RTL narrow-container wrapping and vertical layout. Four native cases pass twice each, asserting ordinary-label containment, multiple wrapped rows, disabled-button skipping, forward/backward Tab order, and retained button identity. Lint/typechecks pass. Added the cases to WebKit selection; runtime remains unverified. Arbitrarily long labels and reviewed visual matrices remain open, and the grouped button-family TODO is still incomplete.

- Implemented the missing M1 ButtonGroup primitive with a required group name, horizontal/vertical token-spaced layout, class/attribute forwarding, and native child keyboard/disabled ownership. Added public exports, metadata, demo, a Loupe fixture, two SSR tests, and two native Chromium cases including delayed hydration replay and retained layout updates. UI build, lint/typechecks, and exhaustive manifest/example validation pass at 74 components and 76 examples. No dependency was added. The first patch failed context verification and made no edits; the corrected patch applied successfully. Visual/RTL/overflow qualification and the remaining button-family API work remain open.

- Added and visually reviewed two 375×750 drawer baselines: obsidian/dark/LTR and paper/light/RTL, with keyboard focus on Close. The wide-content fixture now supports a scoped palette switch. Reviewed edge alignment, title/body alignment, retained footer, scroll-boundary clipping, and focus-ring visibility; neither baseline uses masking or widened tolerances. Native assertions also gate Close text at 4.5:1 and its focus indicator at 3:1 against the drawer surface. Final non-update visual/motion/scroll run: 24 Chromium executions passed, plus lint/typechecks. This is a two-configuration visual gate, not the full theme/density/state or WebKit matrix.

- Qualified disposal of the app-owned drawer persistence controller. The fixture can explicitly remove/remount its owner; the new native case observes requestfailed for the exact pending request and verifies that the replacement starts closed with an empty draft, zero requests, no error, and new DOM identity. All three persistence cases pass twice each, with lint/typechecks passing. The outgoing request is paused before continuation, so this proves client abort/cleanup rather than server cancellation or rollback. WebKit discovery includes 44 cases in eleven files; runtime and hosted CI remain unverified.

- Added a real transport-backed controlled drawer fixture. Two Chromium cases pass twice each for pending dismissal, duplicate proposal coalescing by the app, rejected persistence with retained editable draft/focus, explicit retry, and successful completion after returning to desktop without hiding the desktop sidebar. The app keeps ownership of persistence, error UI, and abort-on-disposal; no data-layer behavior was added to AppShell. The switch interaction uses native focus/Space rather than clicking its visually hidden input. Lint/typechecks pass. WebKit discovery now includes 43 cases in eleven files; runtime and hosted CI remain unverified.

- Added the drawer motion/inline-placement matrix: six Chromium cases pass twice each across LTR/RTL and full/OS-reduced/scoped-reduced motion. Assertions cover edge placement and viewport height, declared drawer/scrim durations, no shell mount animation, retained pane identity through close/reopen, and live OS reduction without losing focus. No production fix was needed. Lint/typechecks pass. Added these cases to the dedicated WebKit selection, now 41 tests in ten files by discovery only; local WebKit remains unable to launch with the current native libraries. Screenshot matrices, broader accessibility, and the full release scope remain open.

- Added a dedicated WebKit entry point and separate Ubuntu 24.04 CI job with dependency installation and retained failure traces. Test discovery verifies 35 WebKit-only cases across nine theme/shell/drawer/history files; the existing Chromium project remains separate. Downloaded the pinned WebKit 2359 browser, but a real local launch failed native dependency validation (including ICU 74 and AVIF 16). No Nix realization or host package installation was performed. Configuration discovery is not runtime qualification, and hosted CI has not been observed. See docs/webkit.md for exact scope and the local environment boundary.

- Extended registered-sidebar restoration with real delayed transport coverage. Six Chromium cases pass twice each: data completing while detached, reopening before data grows, ready short results followed by expansion, keyboard interruption, superseding the pending history target, and the existing same-route/back/forward projection case. Requests are paused then continued to the real endpoint, never fulfilled with a substitute response. Retained pane identity and both offsets are asserted. Lint and root/Loupe typechecks pass; no production change was needed. The short-result expansion initially hit the toggle's hoverable tooltip and now dismisses it after observing its visible state. The previously observed intermittent main-pane failure and non-Chromium/release qualification remain open.

- Fixed competing sidebar projection and route-restoration caches. Registered panes now delegate projection to their shell's route-restoration owner; hidden/detached panes neither overwrite saved offsets nor settle pending restoration against zero-sized layout. Projection restores the current route target synchronously when visible and cancels any superseded scheduled frame. The new native case covers same-route drawer reopening, navigation while detached, history back/forward, both offsets, and return to desktop with retained DOM identity. Final qualification: 48 Chromium executions, ten unit/SSR tests, patterns build, lint, and root/Loupe typechecks passed. An earlier 24-case run had one main-pane desktop/phone restoration failure (expected 700, got 0); it did not reproduce in two isolated or two full-suite repeats, and its cause is not established. Keep that observation open, along with non-Chromium and release qualification.

- Qualified sidebar-owned unsaved-change registrations across responsive presentation with a real Solid router fixture. Four native Chromium cases pass twice each: dirty navigation remains blocked while the drawer is closed and after returning to desktop; saving or explicitly disposing the editor clears the guard; a closed drawer retains the native beforeunload prompt; and a nested discard confirmation cancels only its own layer or completes navigation and removes both overlays. Lint and root/Loupe typechecks pass. This required fixture/test additions, not a production change. Pane-restoration registration across projection, asynchronous persistence, full responsive/visual matrices, other engines, and the remaining M1–M6 scope remain open.

- Extended sidebar scroll qualification with a real wide-content Loupe fixture inside ThemeScope. Four Chromium cases pass twice each: horizontal/vertical LTR and RTL offsets survive drawer reopening and both breakpoint directions; shrinking detached content clamps the saved position, and restoring the larger dataset does not resurrect the obsolete offset. Existing zero-reset and pre-hydration scroll checks also pass. Lint and root/Loupe typechecks pass. Initial attempts were intercepted by the restored toggle's hoverable tooltip; the test now waits for its visible lifecycle and dismisses it with Escape before clicking the neighboring action. No production code change was needed. Content-identity anchors, broader visual/RTL matrices, and non-Chromium qualification remain open.

- Fixed sidebar ScrollArea offsets resetting on desktop/drawer projection despite retained DOM identity. AppShell now keeps a weak, node-keyed offset cache across presentation containers, captures native visible-pane scroll events, adopts pre-hydration positions, and restores offsets when a new container mounts. Hidden/detached zero-offset events cannot overwrite the cache. Two new native Chromium cases pass twice each, covering drawer close/reopen, both breakpoint directions, an intentional return to zero, and scrolling before delayed hydration. Patterns build, lint, root/Loupe typechecks, and five AppShell SSR tests pass. This qualifies fixed-content vertical offsets, not data-reorder anchors, horizontal/RTL projection, or other engines; the grouped sidebar TODO remains open.

- Resolved the stale menu screenshot gate after reviewing both old and regenerated dark/light-RTL images against the current /menus fixture. The added theme-request, changing-scope, and export-removal controls account for the shifted background layout; menu geometry, checked states, focus outlines, and RTL submenu placement remain correct. Updated only the two menu baselines, without masking regions or loosening tolerances. The visual test plus menu and drawer-overlay regressions passed twice without snapshot updates, 24 Chromium executions, retaining contrast and reduced-motion assertions. Broader visual matrices, cross-engine qualification, and the full TODO/release scope remain open.

- Qualified nested sidebar Select across both breakpoint directions and fixed three retained-layer reactivation bugs in the pinned Kobalte core patch: stale Popper wrapper z-index, stale DismissableLayer pointer order, and a paused retained focus scope. The failing native measurements were content/positioner/drawer z-indices 102/100/101, then pointer-events none after visual order was corrected, then focus returning to the parent input. Reactivation now synchronizes/promotes the respective owners with cleanup. Twenty-eight Chromium cases and five unit/SSR tests pass, alongside UI/patterns builds, lint/typechecks, and 73-component/75-example validation. Package installation retained release-age checks and downloaded zero packages. A separate menu full-page screenshot remains stale relative to existing fixture controls; inspected but not rebaselined or counted as passing. Async persistence, responsive scroll anchors, broader nested-widget cases, visual/RTL matrices, cross-engine tests, and full M1–M6 release scope remain open.

- Added controlled phone-drawer qualification using a real app-owned state fixture. Escape, close-button, and backdrop proposals can be rejected without losing the retained draft or focus trap; accepted closure restores the mobile toggle and leaves desktop visibility unchanged. A separate case proves controlled open state survives desktop presentation and reopens on phone without synthetic close proposals. Both new cases and the three existing drawer cases pass in Chromium, with lint and root/Loupe typechecks. This is synchronous controlled-state evidence, not async transport persistence. Nested overlays during resize, responsive scroll handoff, RTL/visual matrices, other engines, and the wider release scope remain open.

- Integrated the retained-owner phone drawer into AppShell. Desktop and phone visibility are independent; local phone state starts closed and resets on desktop return. CSS exposes the correctly expanded responsive toggle and hides desktop navigation before phone hydration. The same sidebar owner projects into normal Dialog chrome with a linked contentId, inherited theme, focus trapping, and Escape handling. Sidebar controls now work without shortcutHelp, requiring ThemeProvider for localized chrome. Thirteen native shell cases, five shell SSR tests, UI/patterns builds, lint/typechecks, and 73-component/75-example validation pass. A stricter resize check exposed CSS blurring the desktop editor before matchMedia dispatch; owned focus tracking now recovers the mobile toggle without stealing main-content focus. Controlled phone rejection, nested overlays during resize, RTL/visual matrices, scroll handoff, and other engines remain open; the grouped M1 sidebar task is not complete.

- Qualified the retained-owner approach for the upcoming phone drawer in /sidebar-handoff. A real Input/Select subtree survives desktop-to-Dialog projection, nested Select Escape restoration, modal close/reopen, Tab wrapping, and return to desktop with its DOM marker and uncontrolled draft retained. One native Chromium case, lint, and root/Loupe typechecks pass. Installed Kobalte focus/scroll lifecycle inspection ruled out treating forceMount as sufficient for this handoff. The qualification fixture is not AppShell drawer integration; first-phone-paint policy, controlled responsive state, shell contexts, trigger linkage, drawer styling, focus handoff, and broader matrices remain open in docs/sidebar-handoff.md.

- Added controlled/uncontrolled AppShell sidebar visibility with defaultSidebarOpen, sidebarOpen, and onSidebarOpenChange. With shortcutHelp enabled and sidebar content present, a localized visible tooltip control and mod+/ binding share the registry. Hidden/inert preserves the existing aside; accepted closing recovers focus from inside it, while rejected controlled proposals retain focus/state. Ten native shell cases and five SSR tests pass, including three new controlled-rejection, focus/scroll/draft retention, and delayed-toggle hydration cases. UI/patterns builds, lint/typechecks, and 73-component/75-example validation pass. The required mobile modal drawer is not implemented by this show/hide contract; responsive focus/portal handoff and full release scope remain open.

- Added developmentOnly shortcut registration policy and AppShell mod+shift+d mode cycling. Production-policy registries omit those registrations entirely; development shells cycle dark/light/system preferences through the existing async theme setter and shared error path. Nineteen registry/shell unit and SSR tests and seven Chromium shell cases pass, including policy-specific inventory/dispatch, focused-input cycling without node/focus/draft replacement, and retained shell hydration/layout behavior. UI/patterns builds, lint/typechecks, and 73-component/75-example validation pass. This is explicit runtime policy evidence, not optimized-production bundle stripping or native macOS qualification. Sidebar toggle/mobile drawer, tooltip association, scaffold integration, and the remaining M1–M6 release scope remain open.

- Added opt-in AppShell shortcutHelp integration using the app-owned ThemeProvider/ShortcutProvider. The shell registers localized global question-mark help, renders a visible fallback trigger and reserved pending slot, and restores the initiating focus target through the generated sheet. Loupe's shell fixture enables it and exposes the character-shortcut preference. Ten native shell/help cases and four shell SSR tests pass, covering retained drafts/slots through refresh, disabled-character fallback, localization, missing-provider errors, and queued server-trigger replay. UI/patterns builds, lint/typechecks, and 73-component/75-example validation pass. The live keyboard test now establishes hydration through the real help control; pre-hydration replay is tested separately. Metadata validation caught missing cross-package imports in the new example, now fixed. Sidebar toggle/mobile drawer, development mode cycling, bound-control tooltip association, scaffold enablement, and full release qualification remain open; no grouped TODO is marked complete.

- Extended explicit keyboard ownership to Select's mounted trigger and open portalled content. Before the fix, character typeahead also fired pane/global actions in closed/open states respectively. Three new native cases cover those failures, modified pane bindings, restoration outside the control, and queued pre-hydration typeahead with retained server trigger identity/focus/value. Seventeen Chromium Select/pane cases pass, including existing form/reset, scoped overlays, refresh, delayed hydration, and five visual/contrast configurations. Four targeted SSR checks, UI build, lint/typechecks, and 73-component/74-example validation pass. The hydration test initially treated buffered `ga` as a fresh `a` search; its follow-up now repeats `g` to test live shortcut suppression. Other composite widgets, shell integration, and full release qualification remain open.

- Fixed DropdownMenu typeahead also firing background character shortcuts. Root and submenu content now acquire explicit optional keyboard ownership from ShortcutProvider while open; unmodified widget keys bypass the registry without suppressing native menu handling, and Control/Meta bindings retain their normal scope policy. Ownership changes cancel pending sequences. Thirty-six native menu/shortcut cases passed, followed by the final five-case pane suite including submenu Escape restoration, modified shortcuts, close/reopen, and hydration. Seventeen targeted unit/SSR checks, UI build, lint/typechecks, and 73-component/74-example validation pass. The new nested test initially raced opening autofocus; it now waits for actual first-item focus before navigation. Select and other nonmodal widgets, shell integration, and broader qualification remain open. No grouped TODO is marked complete.

- Added ShortcutScope, a native DOM boundary for automatic focus-based view/pane activation without maintaining activeScopes in the app. Provider-owned element records resolve the composed event path outer-to-inner; innermost scopes shadow base scopes, while modals override both. Scope disposal releases its ID and pending input. Twenty-six native Chromium shortcut/sheet cases pass, including three new focus cases for nested precedence, input behavior, cross-pane cancellation, remount, modal override, and retained pre-hydration drafts. The new SSR test, UI build, lint/typechecks, and 73-component/74-example validation pass. Shell integration, portalled nonmodal-widget ownership, and broader qualification remain open.

- Added automatic modal shortcut scopes for Dialog/AlertDialog/ConfirmDialog/ShortcutSheet within ShortcutProvider. A unique default or explicit shortcutScope suspends all base scopes; newest modal wins and exact owner removal restores the next scope. The sheet fixture no longer rewrites activeScopes manually. Three new native cases cover nested Escape restoration, pending-prefix cancellation, inner owner removal/remount, and imperative confirmation isolation. Thirty Chromium cases including existing dialogs/navigation guards, five targeted SSR cases, UI build, lint/typechecks, and 72-component/73-example validation pass. ConfirmDialog forwards/documents its inherited prop. Focus-based pane/view selection, shell wiring, and removed-trigger focus recovery remain separate unfinished work.

- Added optional automatic shortcut platform selection after mount. Auto mode creates no provisional wrong-platform registry during SSR/hydration; owned child registrations react when the actual registry becomes available. Explicit overrides remain stable. The real host-browser fixture retains early drafts and focused server nodes, dispatches once, and remounts cleanly. It exposed incidental numeric returns through void callbacks being treated as promises; Promise.resolve adoption fixes that without changing async rejection ownership. Seventeen targeted unit/SSR tests, 20 Chromium shortcut/sheet cases, UI build, lint/typechecks, and 72-component/73-example validation pass. Resolver unit inputs cover Apple/Windows/Linux/unknown hints, not native cross-platform hardware qualification. Automatic focus/modal scopes and shell integration remain open.

- Added ShortcutPending, replacing the fixture-only sequence output with a reusable persistent, localized polite/atomic status region and Kbd hints. Registry pending records now carry platform-formatted displayKeys. Its token-sized single-line slot retains height while idle or active and never takes focus. Fourteen targeted unit/SSR tests and 18 Chromium shortcut/sheet cases pass, alongside UI build, lint/typechecks, and 72-component/73-example validation. Native geometry checks retain region identity and both its own and following-trigger bounds through completion. Shell placement, visual matrices, and actual assistive-technology announcement qualification remain open.

- Expanded ShortcutSheet to five native cases, passing twice (10 executions), with lint and root/Loupe typechecks. A help click queued on server DOM before JavaScript opens one populated sheet, restores the retained trigger on Escape, and preserves an early editor draft. Real delayed removal/restoration of all bindings transitions through the empty state without replacing the dialog or focused close control; Tab stays trapped and no owner warnings/page errors occur. Visual/RTL/contrast matrices, large inventories, automatic scopes, and shell-wide integration remain open. No grouped TODO is marked complete.

- Added live immutable shortcut inventory and ShortcutSheet, grouped by action group with scoped/platform labels, disabled/replaced annotations, and stable row identities. Loupe demonstrates question-mark opening, a visible fallback trigger, explicit modal scope suspension, and live rebinding while open. Fixed native-listener prop access creating unowned Solid memos. Extended the pinned Kobalte core patch to honor prevented modal close autofocus in JSX/JS; previously its trigger focus overrode an explicit returnFocus. Offline installation applied the patch without downloading packages. Seventeen targeted unit/SSR checks, 23 Chromium shortcut/sheet/dialog/navigation cases including unchanged dialog visuals, lint/typechecks, and 71-component/72-example validation pass. Shell integration, sheet-specific hydration/visual matrices, automatic scopes/platform detection, and full accessibility gates remain open. Consumers must carry or supersede the patch.

- Added reactive characterShortcuts preference to ShortcutProvider and per-dispatch core input. Whole-sequence classification disables character-only actions, including Shift punctuation, while retaining modifier/non-character bindings. Preference changes cancel pending input; snapshots expose classification. A real Switch fixture proves keyboard disabling/re-enabling and retains a pre-hydration preference change without replacing the focused server input. Twelve Chromium cases pass twice (24 executions), with thirteen unit/SSR checks, UI build, lint/typechecks, and 70-component/71-example validation. The first fixture used the wrong callback name and a pointer click targeting the covered native input; corrected it to onCheckedChange and real keyboard operation. Persistent app-wide discoverability and speech-input qualification remain open, along with the broader shortcut work.

- Implemented whitespace-separated shortcut sequences with branching, scope capture, a one-second monotonic deadline/timer per stroke, frozen observable pending candidates, and cancellation on Escape, context changes, registration lifecycle, and provider disposal. Ambiguous complete/prefix bindings fail explicitly; exact duplicates preserve the normal collision policy. Exported usePendingShortcut and added a visible fixture status region. Ten Chromium cases pass twice (20 executions); eleven unit/SSR checks, UI build, lint/typechecks, and 70-component/71-example validation pass. A reusable shell indicator, automatic scopes/platform handling, help/tooltips, and user-facing character-shortcut disable/remap remain open. The latter is now an explicit WCAG 2.1.4 TODO; input suppression alone is insufficient.

- Expanded shortcut qualification to seven real Chromium cases, passing twice (14 executions), plus lint and root/Loupe typechecks. Delayed hydration preserves an early draft, focused server node, and queued scope change without duplicate action invocation. Native handled keys and contenteditable text suppress the appropriate bindings. A delayed successful refresh reactively replaces a shortcut while retaining editor identity, focus, and backward text selection. Automatic scope/platform selection, sequences/pending UI, generated help, and shell bindings remain open; no grouped TODO is marked complete.

- Shortcut actions now accept Promise<void>; live rejections report through the provider while disposed registrations consume late failures without notifying replacement owners. Four real Chromium cases pass, including delayed 409 responses after binding/provider removal and remount, with no unhandled page errors and continued shortcut use. The request fixture now consumes its body before rejection; the first run hung on request completion after leaving that body unread. Six unit/SSR checks, UI build, lint/typechecks, and metadata validation pass. Automatic scope/platform handling, hydration, sequences, and help UI remain open.

- Added ShortcutProvider and useShortcut with owner-scoped document listeners/registrations, explicit active scope IDs, composed-path editing detection, already-handled/AltGraph suppression, and synchronous action-error reporting. Matched native defaults are prevented before callback execution. A real Chromium test covers scoped precedence, input text retention, modified actions in inputs, errors, binding/provider disposal, and clean remount. Six unit/SSR cases, UI build, lint/typechecks, and 70-component/71-example validation pass. Automatic focus/modal scopes, hydration coverage, async errors, sequences, help sheet, and baseline bindings remain open.

- Added the pure shortcut-registry core with platform-normalized chords, dev rejection/production last-wins collision handling, inner-scope precedence, browser-shortcut protection, editing/composition/repeat suppression, immutable snapshots, and idempotent registration cleanup/disposal. Four real unit cases, lint/typechecks, UI build, and metadata validation pass. No document listeners or hooks are wired yet; scope activation, native keyboard behavior, sequences, error handling, help sheet, and M1 bindings remain open. See docs/shortcuts.md for the proof boundary.

- Added and visually inspected a four-panel sidebar baseline: dark expanded, light rail, dark RTL rail, and light RTL expanded. All visible destination boxes remain within their sidebar bounds; the focused RTL rail destination and tooltip stay visible. Measured selected-link text/background contrast passes 4.5:1 and focus/base-surface contrast passes 3:1 for these four samples. The visual test passes again without snapshot updates, and lint/typechecks pass. This bounded matrix does not prove every theme, state, density, adjacent-color pairing, or accessibility gate. Mobile drawer and full release qualification remain open.

- Qualified collapsed sidebar focus transitions and initial RTL SSR. A focused disclosure hidden by collapse recovers to People; expanding over a focused descendant of a previously closed group recovers to Team. The query-configured collapsed RTL fixture paints at 48px before JavaScript, exposes nested links, and hydrates the early-focused Settings anchor without replacing it or changing rail geometry; its tooltip inherits RTL. These are behavioral/geometry checks, not reviewed screenshot matrices or an axe audit. Mobile drawer integration, cross-browser and full accessibility/contrast gates remain open.

- Added app-controlled SidebarNav collapsed mode: a 48px rail exposes all nested destination icons, keeps labels/badges accessible, and restores expanded disclosure choices without replacing links. NavItem can retain a supplied LinkTooltip while toggling only its enabled state. Missing icons have a label-code-point fallback. SidebarNav now requires ThemeProvider in both modes. The rail regression verifies retained focused Home, immediate help on enabling it, nested Tab navigation/tooltip, and disclosure restoration. Six unit/SSR cases, a 16-case navigation browser pass, a separate 16-case sidebar/tooltip pass, and lint/typechecks pass. Collapsed SSR/RTL/geometry matrices, focus on hidden disclosure transitions, mobile drawer integration, and full accessibility gates remain open.

- Added LinkTooltip, a native anchor with contextual help that can be disabled without replacing or disabling navigation. Button and link tooltips share portal/layer ownership, focus-on-hydration handling, Escape arbitration, and cleanup. The anchor wrapper separates native MIME type from Kobalte's button-oriented trigger type. SSR, native new-tab activation, scoped RTL help, retained anchors when disabling help, and focused delayed hydration pass. UI build, lint/typechecks, and 69-component/70-example metadata validation pass. Collapsed SidebarNav integration and broader link-tooltip interaction/contrast qualification remain open.

- Added real delayed clear/repopulate sidebar cases. Clearing all sections recovers keyboard focus to the named root, Tab exits to the next outside link, and repopulation leaves the fallback focused until Tab enters Home. Outside focus is preserved. Geometry assertions exposed a zero-height empty fallback despite a computed solid outline; an empty-only control-height minimum now provides visible focus area. Eight sidebar Chromium cases and lint/typechecks pass. Collapsed mode/link tooltips, cross-parent move qualification, and full visual/accessibility matrices remain open.

- A real delayed sidebar group-removal regression reproduced lost focus after its inner NavList recovery owner was disposed. Added persistent sidebar-boundary recovery across native links and disclosure buttons, preferring surviving controls in prior navigation order and preserving deliberate outside focus. Six Chromium sidebar cases and three unit/SSR cases pass, including reordered nested identity, removed-group fallback to Home, same-route disclosure continuity, and delayed hydration. Empty-sidebar fallback, cross-parent moves, collapsed mode, and visual/accessibility matrices still require qualification.

- SidebarNav now keys sections and same-parent entries by stable IDs, reading replacement objects reactively without replacing same-kind owners. Real delayed refresh retains focused link identity and manually expanded groups. A new regression exposed same-route refresh reopening a deliberately closed active group; memoizing the active ID prevents object invalidation from masquerading as navigation. Four sidebar browser cases pass three repeats (12 executions), alongside three unit/SSR cases, lint/typechecks, and metadata validation. Cross-parent moves intentionally create new owners; reorder/removal recovery, collapsed mode/link tooltips, and visual/accessibility matrices remain open.

- Added SidebarNav sections and recursive disclosure groups using native NavList/NavItem links and Collapsible. App-supplied accepted pathname selects one longest segment-aware match; external destinations are excluded, duplicate IDs and empty labels/destinations fail, and active ancestors open in SSR and on subsequent route changes. Three unit/SSR cases and two real route/keyboard/hydration browser cases pass (six executions over three repeats). Patterns build, lint/typechecks, and 68-component/69-example metadata validation pass. Collapsed icon mode/link tooltips, refreshed-object identity reconciliation, removal/focus qualification, and visual/accessibility matrices remain open, so the grouped SidebarNav TODO is not complete.

- Added persistent decorative icon and noninteractive badge slots to NavItem as SidebarNav building blocks. Labels remain visible, badges contribute accessible text, native anchors retain navigation behavior, and logical flex placement mirrors in RTL. Three SSR cases, seven Chromium navigation cases (including unchanged visual baseline), UI build, lint/typechecks, and exhaustive metadata/example validation pass. Real delayed count updates preserve focused link identity, and delayed hydration retains server link/icon nodes. SidebarNav sections, nesting, active-route matching, collapsed presentation, and a link-compatible tooltip remain unfinished; its grouped TODO stays open.

- Expanded phone restoration to eight real Chromium cases, all passing three repeats (24 executions). Delayed row growth restores the saved offset, short results settle without revival on later expansion, and wheel/touchscreen-tap input cancels pending restoration. Shell disposal restores both initially automatic and initially manual native history settings. Lint/typechecks pass. These additions required tests only, not another runtime change. Touch swipes/momentum, physical devices, mobile drawer, and cross-browser qualification remain open.

- Added phone document restoration using the existing shell/router owner, with separate desktop/phone coordinate keys and native document event handling. The responsive regression exposed CSS clamping the outgoing viewport before its media callback; saves now verify that their viewport remains active. Twenty combined Chromium shell/restoration/navigation cases pass, including early phone hydration and responsive round trips. Eight AppShell/cache unit cases and lint/typechecks pass. Delayed phone responses, touch/cross-browser coverage, mobile drawer, title synchronization, and packaged adapter work remain open.

- Qualified full-shell disposal during pending restoration with a real delayed response. Remounting starts with fresh offsets and subsequently restores its own navigation state without page errors. All fourteen restoration/navigation-guard Chromium cases, lint, and root/Loupe typechecks pass. This is behavioral owner-isolation evidence, not a heap-leak audit. Mobile document restoration, title synchronization, and the packaged router adapter remain open; no grouped TODO is marked complete.

- Added optional router entryKey to restoration and pending-navigation identity. Loupe generates bounded per-depth history generations because pinned Solid Router's public location.key is empty; replacement forward branches get fresh keys. Thirteen restoration/guard Chromium cases pass, with same-URL entry isolation and permanent-shrink revisit passing three additional repeated runs each. Patterns build and lint/typechecks pass. The adapter's private depth seam needs upgrade qualification; mobile document scrolling and full shell disposal remain open.

- Added explicit restoration readiness: AppShell contentReady and a ready accessor for custom panes, defaulting to ready. Incomplete content may grow toward a saved target; completed short content settles at its clamped boundary and cannot jump later. Size observation stops on settlement/interruption. All nine restoration Chromium cases pass twice (18 passes), including a real permanent-shrink response, later expansion, and revisit. Patterns build, lint/typechecks, and 67-component/68-example validation pass. Same-URL history identity, mobile document scrolling, and full shell disposal remain open.

- Expanded pane fixture to a two-axis sidebar under a switchable direction scope. Eight restoration Chromium cases pass; the extended RTL case passes three repeated runs restoring negative horizontal/positive vertical offsets across history and a real delayed two-dimensional expansion. Lint/typechecks pass. Permanent shrink, same-URL history-entry identity, mobile document scrolling, and full shell disposal remain open.

- Bounded restoration storage to 256 LRU pane/location pairs, including dynamic pane IDs; immutable finite-offset snapshots and collision-safe keys pass five unit cases. First mounts without saved positions no longer schedule a redundant scroll reset. The remount fixture now observes app refresh before navigation, after tracing an initial-hydration race where Page A was left before mount hooks could record it. All seven restoration cases pass three repeated Chromium runs. Patterns build, lint, and typechecks pass.

- Expanded restoration qualification to seven passing Chromium cases, adding native wheel/PageDown cancellation and sidebar removal during pending restoration followed by same-ID remount and continued navigation. Lint/typechecks pass. This proves remount behavior, not a heap/listener-leak audit. Permanent shrink, same-URL history identity, RTL/mobile, cache bounds, and complete shell disposal remain open.

- Pane restoration now passes four Chromium cases repeated three times, covering delayed hydration, real delayed row growth, pointer cancellation, independent panes, and refresh continuity. Repetition exposed a stale-offset race when history navigation preceded the final scroll event; a non-blocking before-navigation capture fixes it. Patterns build, lint, and typechecks pass. Same-URL entry identity, permanent shrink, other interruption modes, RTL/mobile, cache bounds, and disposal remain open.

- Added owner-scoped per-pane restoration, automatic main-pane registration, and public usePaneScrollRestoration for additional panes. A persistent-shell Chromium case restores independent main/sidebar offsets across back/forward, retains viewport identity, and preserves scroll during refresh. Fixed shell slot resolution to run under its contexts; three AppShell SSR cases pass. Patterns build, lint/typechecks, and metadata pass. Delayed content/user interruption, same-URL entry identity, mobile/RTL, cache bounds, and disposal qualification remain open; see docs/pane-restoration.md.

- Loupe now exercises the shell contract through its existing Solid router dependency, without adding dependencies. Three navigation browser cases pass three repeated runs each: cancelled/confirmed links, an unrelated app blocker, and native browser-forward cancellation/retry. The forward case reproduced and fixed a synchronous-only bypass that expired before popstate. Lint/typechecks pass. Dedicated package adapter publication still awaits optional-peer approval; title synchronization and pane restoration remain open.

- Added the public router-adapter contract and AppShell's localized dirty-navigation confirmation. The guard blocks competing pending attempts, aborts on disposal/location change, preserves other blockers, and shows a generic failure dialog on confirmation/retry rejection. Four pure guard cases pass. Concrete Solid adapter browser qualification, title synchronization, and pane restoration remain open; optional-peer dependency approval was requested before adding the adapter dependency.

- Added AppShell dirty-state context and owner-scoped useUnsavedChanges registration with a conditional beforeunload guard. Four registry/AppShell unit/SSR cases, patterns build, lint/typechecks, and metadata validation pass. The reload test raced hydration; observing app dirty state establishes the guard before reload. Three native Chromium scenarios pass three consecutive runs each, covering cancellation/draft retention, save, multiple editors, and disposal. A separate delayed-JS case retains an early draft and registers it after replay. Router blocking/title/scroll restoration and the grouped TODO remain open. See docs/unsaved-changes.md.

- Added owner-scoped useOnlineStatus/useIsWindowFocused hooks with unknown SSR/initial hydration state and no implicit StatusBar/data-layer wiring. UI build, one SSR unit case, lint/typechecks, and 67-component/68-example validation pass. Browser offline transitions, disposal/remount, and delayed hydration pass; native tab-focus qualification is recorded in tests/browser/browser-status.spec.ts. The full useUnsavedChanges/navigation-blocking TODO remains open. Eight theme/hydration browser cases also pass following the portal-target correction.

- The 80px Toolbar fixture reproduced 33px of trigger overflow, fixed by container-relative label wrapping. Its RTL reopening regression exposed portal children being removed by reactive attribute spreads on theme updates. Root and scope targets now update explicit attributes without reconciling overlay-owned children. Twenty menu/Toolbar Chromium cases pass, plus two direct delayed-request tests preserving open root/scoped menu DOM, focus, and action invocation across a theme update. UI build and 67-component/68-example metadata validation pass. Full contrast/cross-browser qualification remains open.

- Toolbar geometry regressions reproduced 68px of scrollable hidden-group overflow and a first-action focus ring clipped by 4px. Hidden groups now use zero-size clipped wrappers around measurable inner content; allocation reserves the full ring clearance. Twelve Toolbar Chromium cases pass without snapshot updates, including zero narrow LTR/RTL overflow and visually reviewed dark/light RTL focus baselines. Patterns build, lint/typechecks, eight unit/SSR cases, and 67-component/68-example metadata validation pass. Below-trigger-width layouts, full contrast matrices, and cross-browser qualification remain open.
- Clearing every action while overflow was open reproduced a stranded empty menu. Toolbar now closes empty overflow and coordinates fallback focus through the close-autofocus event, preserving outside focus. Ten Toolbar Chromium cases pass, including clearing, restoring actions and invoking Save again, and moving outside focus while a clear is pending. Patterns build, lint/typechecks, and 67-component/68-example metadata validation pass. Scrollbar/focus-ring geometry, narrowest layouts, visual/contrast and cross-browser qualification remain open.
- Removing the focused action from an open Toolbar overflow menu reproduced lost focus. Added shared owner-cleaned DropdownMenu content recovery, scoped to each root/submenu layer and preserving enabled-item order. Real delayed-response cases now continue keyboard invocation after removal in both Toolbar overflow and a nested submenu. All 15 menu/Toolbar Chromium cases pass, including existing hydration, checked/radio state, nested Escape, and scoped-dialog behavior. UI/patterns builds and lint/typechecks pass. Entire-menu removal/emptying, visual/focus geometry, contrast and cross-browser qualification remain open.
- Toolbar reorder/disable regressions pass with retained focused DOM and correct updated keyboard order. Open-menu resizing then exposed premature group promotion that hid the trigger before close-focus restoration. DropdownMenu now exposes its close-autofocus lifecycle event; Toolbar freezes allocation through that event instead of guessing a delay. Seven Toolbar Chromium tests pass, including resize/action invocation/focus restoration and subsequent promotion after focus leaves. UI/patterns builds, lint/typechecks, and 67-component/68-example metadata validation pass. Open-menu removal, scrollbar/focus-ring geometry, visual/contrast and cross-browser qualification remain open.
- A real delayed-response Toolbar regression reproduced lost focus when the selected inline action was removed. The component now retains the previous enabled-action order and recovers to a surviving successor/predecessor or its own focusable root, without taking focus back from outside controls. Five Toolbar browser tests pass, including removal, empty fallback outline, outside-focus protection, existing resize/RTL/overflow behavior, and hydration replay. Patterns build, lint/typechecks, and eight targeted unit/SSR tests pass. Dynamic reorder/disable and open-menu mutation/resize cases remain open alongside visual/cross-browser qualification.
- Added the initial public Toolbar with stable-ID action/toggle groups, measured whole-group overflow through scoped DropdownMenu, a persistent independent filter row, roving enabled-button focus, RTL arrows, and resize transfer to the overflow trigger. SSR starts with accessible overflow; focus-held promotion prevents queued hydration activation from losing its action set. Three Chromium tests pass, including an actual queued Save invocation, inline/overflow toggles, retained filter DOM/drafts, single Tab stop after resize, and native filter editing. All libraries/Loupe build; lint/typechecks, eight unit/SSR cases, and 67-component/68-example validation pass. Dynamic removal/reordering, open-menu resize, scrollbar/focus-ring geometry, narrowest layouts, and visual/contrast/cross-browser qualification remain open.
- Began Toolbar with an internal whole-group overflow allocator. Six unit tests pass, including fractional/exact boundaries and a 1,001-width invariant sweep; lint/typechecks pass. The allocator reports the overflow trigger's minimum required width rather than silently losing actions in a too-narrow pane. This is layout-algorithm evidence only: the public component, DOM measurement, scoped menu wiring, roving focus, hydration/resize behavior, and visual qualification remain open. See docs/toolbar.md.
- LoadingState now has native-clock regression coverage for the 200ms cold and 500ms refresh thresholds, including switching away from a pending cold timer. A disposal case checks detached fallback markup remains unrevealed beyond the deadline and a fresh owner works. All five LoadingState browser cases passed three repetitions (15 passes), with lint/typechecks passing. Visual/contrast and cross-browser qualification remain open; no broad M1 task was marked complete from this targeted evidence.
- Added LoadingState with explicit idle/cold/refresh phases, a layout-matched app fallback, delayed 200ms cold skeleton and 500ms refresh bar, persistent content, scoped reduced motion, and owner-cleaned timers. Three real Chromium tests pass for matched cold geometry, accepted/failed refreshes, retained DOM/drafts, delayed hydration, and immediate-refresh cancellation. Per-animation-frame computed-style samples verify content stays visible without skeletons during refresh and observe both animated/static 2px progress; they avoid polling past the short progress window. Builds, lint/typechecks, two SSR tests, and 66-component/67-example validation pass. Exact timing/disposal, visual/contrast, cross-browser qualification, and Toolbar remain open.
- Added regional ErrorState for not-found/server/permission-denied outcomes, scoped titles, safe descriptions, and app-owned retry. Pending retries suppress duplicate keyboard activation; rejection produces generic localized feedback and preserves adjacent content, action drafts, and region identity. Owner disposal ignores late completion. Four Chromium tests pass without snapshot updates using a real delayed HTTP endpoint, including retry recovery, disposal, hydration replay, unchanged adjacent-draft bounds, mobile layout, and a reviewed dark/light RTL baseline. Builds, lint/typechecks, two SSR tests, and 65-component/66-example validation pass. LoadingState/Toolbar and broader accessibility/cross-browser qualification remain open.
- Added PageHeader with a 44px minimum title/action row, semantic heading level, named breadcrumb/action/tabs slots, wrapping logical layout, and persistent slot controls. Six targeted PageHeader/AppShell browser tests pass without snapshot updates, including reviewed dark/light RTL geometry, native action focus, retained selected tabs/drafts, delayed hydration replay, and the actual shell header integration. All libraries/Loupe build; lint, typechecks, eleven targeted SSR/metadata tests, and 64-component/65-example validation pass. Cross-package examples now support explicit typechecked import preambles, with missing-export and deduplication regressions. Toolbar/regional-state patterns, full contrast and cross-browser qualification remain open.
- Added the patterns StatusBar with app-owned connection/task props, validated counts, scoped English/override messages, explicit-locale number formatting, persistent action children, compact logical layout, and native hidden support. Connection/task announcements are separate from frequent counts. Six targeted StatusBar/AppShell browser tests pass, including delayed hydration, offline app-driven retry, retained focus/drafts, mobile overflow, and a visually reviewed dark/light RTL baseline without snapshot updates. All libraries and Loupe build; lint, typechecks, six targeted SSR/message tests, and 63-component/64-example metadata validation pass. Full contrast/assistive-technology/cross-browser qualification and the rest of the grouped PageHeader/Toolbar/regional-state task remain open.
- Chromium forced-colors emulation reproduced ScrollArea thumbs becoming translucent white. A scoped media rule now uses system Canvas/CanvasText/Highlight colors without disabling browser color adjustment. Fourteen targeted shell/scroll tests passed, then the strengthened forced-colors case passed with live light/dark palette changes and distinct track/thumb checks. Lint and workspace/Loupe typechecks pass. Physical OS high-contrast palettes and cross-browser coverage remain unverified.
- ScrollArea capture cleanup now has real delayed-response regressions for both disappearing overflow and whole-owner disposal. The latter records native capture before removal, checks its release on the detached track while the pointer remains down, and exercises a newly mounted viewport. Thirteen targeted ScrollArea/AppShell Chromium tests pass without snapshot updates; lint and workspace/Loupe typechecks pass. Forced-colors, broader contrast/visual and cross-browser qualification remain open, so the shell task is still unchecked.
- A two-axis, bordered, narrow ScrollArea regression reproduced tracks positioned 838px outside their viewport. Tracks now use the viewport's inner border box instead of the surrounding frame; both-axis corner placement follows live RTL changes, and drag distances normalize ancestor scaling. Eleven targeted ScrollArea/AppShell Chromium tests pass, including a visually reviewed RTL baseline without snapshot updates. Production builds, lint/typechecks, and two ScrollArea SSR tests pass. Disposal/capture, viewport-only transforms, forced-colors/full contrast and cross-browser qualification remain open; the shell task remains unchecked.
- ScrollArea now uses measured 8px overlay thumbs instead of platform thin bars, retaining its native viewport/ref and adding a constrained outer frame. Vertical and RTL horizontal pointer capture, track jumps, resize response, zero native gutter, and idle fade pass in the targeted browser run alongside native scrolling, hydration retention, and desktop/mobile AppShell regressions. All libraries/Loupe build, lint/typechecks, two ScrollArea SSR tests, and 62-export/63-example metadata validation pass. Two-axis corners, custom borders/transforms, dynamic-direction/disposal cases, visual/forced-colors and cross-browser qualification remain open; see scroll-area.md. The full shell task remains unchecked.
- Added the planned sheen-patterns workspace package and initial AppShell layout, with once-resolved header/sidebar/status slots, semantic landmarks, constrained independent desktop panes, and CSS prepaint document locking. Three Chromium tests and two SSR tests pass, covering fixed chrome, refresh-retained drafts/scroll, delayed-JS pane identity, and phone document-wheel scrolling. Mobile testing exposed both an axis-rule precedence conflict and sidebar scroll-chain containment; the responsive rules now explicitly resolve both. Loupe's broad main styling excludes the shell grid cell. All three libraries and Loupe build; lint/typechecks and 62-export/63-example metadata validation pass. Added a separate build-time metadata package entry and linked only local workspace dependencies, with no new external versions. Mobile sidebar is still interim inline flow; drawer, router/restoration, shortcuts, overlays, visual/cross-browser and full release qualification remain open. See app-shell.md; the shell TODO remains unchecked.
- Added the initial native ScrollArea viewport with required region naming, keyboard/wheel scrolling, axis constraints, native refs, thin themed bars, and disposal-owned 800ms scroll-idle state. Two SSR tests and three Chromium tests pass, including RTL horizontal coordinates, no horizontal document overflow, refresh retention, and delayed hydration preserving drafts and pre-hydration scroll. PageDown initially exposed a two-pixel test sampling difference while Chromium's native animation was still running; measuring refresh retention after the observed idle boundary resolves that distinction without relaxing position equality. UI build, lint, typechecks, and 61-export/62-example metadata validation pass. The pinned Kobalte package has no ScrollArea export. Consistent 8px overlay thumbs/fade and AppShell remain unfinished, documented in scroll-area.md; the shell task remains unchecked.
- The grouped navigation implementation task is checked: Tabs, Breadcrumb, NavList/NavItem, Collapsible, and Accordion now have component metadata/demos, SSR/keyboard/hydration coverage, and reviewed relevant visual states. The latest NavList/Breadcrumb run passes nine Chromium tests without snapshot updates and four SSR tests. A real-request regression first reproduced removed-link focus loss; both components now share disposal-owned recovery for retained/reordered links, nearest surviving links, and a visibly focused empty root without URL activation or outside-focus theft. Existing navigation screenshots and hydration regressions still pass. All-state/all-theme contrast, shared Link consolidation, cross-browser/full-suite and release qualification remain independent unfinished tasks, not implied by this checkbox.
- Added NavList/NavItem with named native navigation/list semantics, real anchors, app-owned current-page state, and logical current/focus styling. Two SSR tests and three Chromium tests pass: native keyboard/hash navigation, a real new tab, refreshed anchor identity, delayed hydration retaining links/drafts, and a reviewed dark/light RTL screenshot without snapshot-update mode. A 375px check confirms no horizontal overflow. Both libraries and Loupe production builds, lint, typechecks, and metadata validation of 60 exports/61 examples pass. Dynamic removal/reorder focus, shared Link consolidation, full state/contrast and cross-browser qualification remain open; see navigation.md. The grouped navigation task remains unchecked.
- Tabs qualification now includes nine passing targeted Chromium tests. A visually inspected dark/light RTL/compact vertical/high-contrast baseline passes without snapshot-update mode. Native Tab/Shift+Tab tests verify consistent panel entry and skipping inactive retained panels. Computed contrast checks pass for eight enabled tabs in that bounded fixture, including AAA text in the contrast sample and both focus-ring adjacencies at 3:1. Both libraries and Loupe production builds, lint, and typechecks pass. All-theme/all-state contrast and full-suite/cross-browser qualification remain open; NavList/NavItem are still missing, so the navigation task remains unchecked.
- Added Tabs with stable data-driven options, controlled/uncontrolled selection, automatic/manual activation, horizontal/vertical layouts, and retained hidden/inert panels. Two SSR tests and six targeted Chromium tests pass, including explicit server associations, disabled skipping, controlled rejection, RTL/vertical arrows, label-refresh identity, and delayed hydration preserving drafts and queued selection. New real-request regressions first reproduced hidden-panel focus loss and first-tab selection after middle-tab removal. Recovery now preserves outside focus, selects the nearest enabled neighbor, retains that fallback when removed items return, and provides visible empty-root focus. UI build, lint, and typechecks pass after these changes; metadata was previously validated with 58 exports/59 examples. Panel tab-stop refinement, visual/contrast and full-suite qualification remain open; see tabs.md. No navigation milestone is newly checked.
- Accordion now passes three SSR tests and nine targeted Chromium tests for controlled/uncontrolled expansion, disabled-header skipping, native panel-input editing, delayed hydration with retained server DOM/drafts, async collapse focus restoration, nested keyboard ownership/cancellation, reordered drafts, removed-section fallback, and empty-root focus. Two keyboard regressions failed before the root DOM keyboard boundary correction; removing a focused section failed before root-owned focus recovery. A visually inspected dark/light RTL baseline passes without snapshot-update mode, with no initial animation and scoped/OS reduced-motion checks. Both libraries and Loupe production builds, lint, and typechecks pass. Metadata was validated with 57 exports and 58 examples. All-theme component-state contrast, full-suite and cross-browser qualification remain pending; the grouped navigation TODO remains unchecked.
- The latest `pnpm check` passes builds, lint, typechecks, 112 unit tests, private-theme consumption, and 55-component/56-example metadata validation.
- The latest full Chromium run passes all 101 tests without snapshot-update mode. Two earlier runs had 69 passing and 16 failing cases, including early overlay activation, native form state, and lost drafts. The pending-lazy hydration-completion guard in the pinned Solid patch resolves those failures without weakening their assertions. A new route-only request barrier reproduced the replacement before the fix and now proves retained server DOM/drafts and exactly-once click replay. See hydration.md.
- Nine Toaster behavior cases pass: focus-paused remaining lifetime, native tab inactivity, queued/hover lifetime, pending/rejected actions, early trigger replay into the ready portal, scoped modal Escape ownership, successful-action focus restoration, deliberate external focus, and dismissal during a real request. Three SSR tests pass, including duplicate-presenter rejection.
- Inactivity uses full Chromium and disables Playwright's forced-focus override after navigation, because navigation reinstalls it for the new document. Real tab switching then produces native blur/focus events. Assertions establish inactive and active document state before checking paused expiry and resumption; no synthetic application events are dispatched.
- The delayed-lazy hydration regression and native inactivity regression each pass five additional repetitions, ten passes total. Final lint and workspace/app typechecks also pass after the test changes.
- Two additional Toaster tests cover reviewed dark-stack/light-RTL snapshots, logical viewport placement, retained sibling identity, removed-opener focus fallback, reduced motion, and actual exit-animation lifetime. The exit test pauses the native animation, asserts the retained card is inert/accessibility-hidden and the queue does not advance, then resumes it and verifies promotion. Cleanup awaits the browser animation promises rather than parsing computed CSS or estimating a timer.
- Starting a focused action transfers focus to Close before disabling the action; successful removal restores the opener only if focus still belongs to that card. A real-HTTP regression failed before this fix and passes afterward. Deliberate external focus remains untouched.
- Three further browser cases pass for presenter disposal/released lease/canceled timers, owner disposal during a real pending request without cross-queue effects, and changing-limit lifetime retention. The latter failed before lifetime accounting moved from card-local variables into presenter-owned records keyed by notification ID. A new presenter starts a fresh presentation session; reducing the limit within a mounted presenter preserves consumed time.
- The grouped Toast/Toaster and optimistic affordances task is checked. The current complete package check passes, and final lint/typechecks pass after compacting the expanded fixture controls. That fixture adjustment removed unrelated controls from behind transparent screenshot margins; no reviewed snapshots were changed. Full accessibility, cross-browser, table/chart/tooling/dogfood, and release scope remains open.

## Foundation

- Strict TypeScript/pnpm workspace, independently built token/UI packages, SolidStart Loupe client and SSR builds, matching CI commands.
- Token schema/compiler with reference cycles and versioned defaults; six themes and twelve independent accents. All 144 theme/mode/accent combinations pass text, status/market, and control contrast checks, including AAA text for contrast. Rendered solid/soft controls additionally pass the current focus/surface matrix. Full component-state, visual, and chart-palette qualification remains open.
- Thirteen-step chromatic OKLCH ramps, expanded semantic tone foregrounds, warm Vellum reading surfaces/larger prose, and untinted contrast-theme soft surfaces. Low-specificity theme selectors allow density/radius/motion modifiers to take effect.
- Scoped density, radius, motion, direction, explicit-locale Intl formatters, and inherited message overrides. Dialog close labels use the scoped translation for both visible text and accessible name.
- Colocated metadata/demo schemas for all fifty-five current exported components. The generated manifest enumerates all props, including inherited Solid/HTML attributes. Build-time ts-morph checks authored prop coverage, stale metadata, demo exports, defaults/control values, and every extracted example. Metadata stays outside the runtime component entry point.
- Stack, Row/Cluster, Grid, Container, Center, and Spacer use role-based spacing and logical CSS, preserve native attributes/classes and DOM order, and render without browser globals. Container stays full-width instead of introducing a centered document column. The `/layout` fixture covers actual density/RTL/grid changes and retained drafts.
- Text, Heading, Code, Kbd, Badge, and Tag emit semantic markup with type/color roles. Tag removal is app-owned and uses a localized native button. The `/typography` fixture covers status roles, solid/soft/outline variants, tabular figures, type-role changes, disabled removal, and retained DOM/drafts. Component CSS is layered below utilities, with actual class-override browser assertions.
- Surface/Card redeclare focus offsets for their own fills; cards default to no shadow. Separator wraps the real Kobalte primitive with decorative/orientation controls. Alert is urgent and persistent without moving focus; Callout is a static note. The `/surfaces` fixture covers all base surface variants, nested cards, notice roles, RTL borders, and retained drafts.
- Provider/scopes and root-only persistence; prepaint and scoped scripts; cookie SSR with a real persistence endpoint.
- Button/Input/Dialog prototypes, contextual portals, provider-owned overlay order, and real browser fixtures at `/`, `/scopes`, and `/cookie`.

## Verification performed

- Latest `pnpm check`: both libraries and Loupe build, ESLint, workspace/app typechecks, 112 unit tests, isolated private-theme package consumption, and metadata validation of fifty-five exports/fifty-six extracted examples. Vitest compiles TSX, including Kobalte's JSX exports, with Solid's real SSR transform; no DOM mocks are involved.
- Private-theme checks exercise emitted public exports and declarations, CSS entry resolution, and invalid-version/missing-token/color rejection with no workspace source aliases. Unit tests additionally cover schema-v2 tone migration through private semantic roles. This is not npm publication proof.
- Chromium browser suite: dark startup, scoped dialog colors, draft/DOM retention, delayed hydration with pre-hydration color checks, cookie persistence and offline failure, nested Escape/focus restoration, scope setters/inheritance/null, and a real sandboxed-storage + nonce-CSP bootstrap case.
- Latest full Chromium run: 74 tests passed without snapshot-update mode, including nested DropdownMenu focus/ownership/keyboard replay, Tooltip/Popover focus/hover/scoped dismissal, dialog ownership/focus/confirmation disposal, Select keyboard replay/refresh retention/scoped overlays, RadioGroup keyboard/RTL and delayed hydration, grouped-choice reorder/removal focus, external form reset, Checkbox/Switch behavior, shared-state regressions, choice-control visuals/contrast, and form placeholder contrast. The rendered control contrast matrix checks 8,064 combinations (six themes, two modes, seven surfaces, eight solid/soft tone controls, twelve accents). Modifier tests assert actual computed dimensions/durations, nested reset behavior, OS reduced motion, translated accessible labels, and locale/direction updates without remounting.
- Layout browser coverage includes token spacing, wrapping, centered geometry, keyboard order, native hidden attributes, RTL, preserved drafts, delayed hydration DOM identity, and unchanged geometry. The typography matrix adds 384 rendered badge/tag samples and tag focus/text checks across all six themes and both modes. Keyboard Enter/Space removal and disabled-button tab skipping pass. Four layout/typography screenshot baselines were visually inspected and passed without snapshot-update mode. Hosted CI and cross-platform font rendering remain unverified.
- Subsequent targeted surface run: three Chromium tests passed, covering 168 rendered control/surface text and focus combinations, separator geometry/semantics, RTL notice borders, alert focus retention, and delayed hydration. Two complete surface screenshots were visually inspected and passed without update mode. The initial clipped captures were replaced only after fixing the fixture's scrolling boundary; viewport assertions now guard the top and bottom of each captured sample.
- `nix eval --impure --read-only`: native browser library path and generated Pog wrapper text.
- Generated browser wrapper passes `bash -n` and ShellCheck; `jfmt --ci default.nix` passes. The full Nix derivation was not realized.
- Browser tests use the lockfile's Chromium revision 1243 and the native libraries declared in default.nix. Playwright's initial install automatically removed unused revision 1208 cache entries, which can be downloaded again. Browser cache GC is now disabled in the Nix environment.

## Next work

- The M0 comparison selected jade as the default accent. Obsidian remains the default theme and dark remains the fresh-install mode; all six themes and twelve accent presets stay independently switchable.
- Finish remaining M1 theme/token and bundle requirements, then expand the M1 component/shell inventory using the metadata/demo checks. Manifest-driven Loupe pages and the M5 generated context/CLI/lint/assay scope remain unfinished.
- Keep table/chart/refresh-continuity, full accessibility and cross-browser gates, generated tooling, benchmark calibration, and viz/metron qualification in scope. They are not represented by the current narrow passing suite.
- Actual publishing and external-repository mutations remain separate execution boundaries in TODO.md.

## Breadcrumb progress

- Added a localized navigation landmark with a nonempty, validated, stable-ID path. Ancestors are native links; the final item is current-page text. Label refreshes preserve link nodes, and normal Tab/Enter/browser URL behavior is retained. Separators are decorative CSS with logical direction, not spoken text or menu controls.
- Two SSR tests cover landmark/current semantics, actual URLs, and invalid hierarchies. Three Chromium tests cover native navigation, refreshed DOM/drafts, focused-link hydration with delayed scripts, narrow viewport wrapping, and a reviewed dark/light RTL screenshot. Visual inspection caught and corrected an over-rotated RTL separator before the baseline was accepted.
- The full 97-test browser suite passes without updating snapshots. The grouped navigation TODO stays unchecked until Tabs, NavList/NavItem, Collapsible, and Accordion land. Shared Link extraction, removed-ancestor focus behavior, and broader navigation accessibility/contrast matrices remain follow-up qualification work.

## Collapsible progress

- Added controlled/uncontrolled disclosure state with native disabled/keyboard behavior and stable SSR controls associations. Kobalte owns activation; Sheen's persistent content wrapper retains drafts, becomes inert/accessibility-hidden immediately on close, and animates grid rows without dimension measurement or mount animation. Reduced motion is inherited from both scope and OS.
- Two SSR tests cover default-open, controlled-false, disabled state, content retention, and IDs. Four browser tests cover Enter/Space, rejected controlled changes, native form-button semantics, refreshed labels/drafts, focus restoration after a real delayed external close, deliberate outside focus, pre-hydration replay, and a reviewed dark/light RTL screenshot. Existing server inputs survive hydration without replacement.
- The complete package check and all 101 Chromium tests pass. Disclosures.md documents that retained children keep their effects and native form participation; collapse is not an authorization boundary. The grouped navigation task remains unchecked for Tabs, NavList/NavItem, and Accordion, with broader accessibility/contrast and cross-browser gates still open.

## Toast card progress

- Added the stateless Toast message card with labeled/associated content, safe failure copy, localized close/retry/loading controls, pending action suppression, and token-based raised surfaces. Cards use a group role; Toaster owns live announcements and timing.
- Three SSR tests cover accessible associations, native controls, pending/dismissal behavior, and exclusion of raw transport details. Two real-HTTP Chromium tests cover rejected actions, explicit retry, stable action nodes, independent scoped dismissal, RTL/messages, and focused-control identity through a message refresh.
- Two visually reviewed card snapshots cover dark success and light/RTL failure. The visual test uses real Tab navigation and asserts a drawn focus outline before checking text/focus contrast and viewport bounds. Programmatic-focus-only captures were replaced because they did not demonstrate the keyboard ring.

## Notification controller progress

- Added the owner-local `createToaster` controller with immutable ordered snapshots, opaque instance-local handles, complete-message replacement, and isolated dismissal/clear/disposal. It creates no DOM, timer, global queue, or application data operation.
- Eleven real unit tests cover isolation, frozen option snapshots, duration/text validation, persistence defaults, duplicate-action suppression, retained failure and explicit retry, stale success/rejection after replacement, and late rejection after dismissal/clear/disposal. Execution tokens prevent previous actions from changing a newer notification or action.
- Toaster supplies scoped portals, announcements, pauseable visible-time expiry, and transient motion. See current qualification above and notifications.md for the public controller and error-ownership contract.

## Optimistic operation progress

- Exported `optimistic(apply, commit)` with synchronous app-owned apply, asynchronous commit/result propagation, and exactly-once rollback after commit failure. Rollback is awaited; an AggregateError preserves both causes if rollback also fails. No cache, transport, implicit retry, notification, or post-success undo is owned by the helper.
- Seven unit tests cover immediate application, success, apply failure, synchronous commit failure, asynchronous rollback, dual failure, and concurrent app-owned inverse operations preserving newer edits.
- Two targeted Chromium tests pass against the real bounded `/api/optimistic` endpoint. They cover accepted commit and explicit undo, rejection and retry, retained input/focus/DOM, pending state without replacement, and a newer independent edit surviving rollback. The endpoint is a stateless transport fixture, not persistence proof.
- The sheen notification controller is owner-local. It does not expose Kobalte's module-global toast store or allow one scope's cleanup to clear another scope.
- See optimistic-operations.md for atomic apply, inverse/version ownership, ambiguous server outcomes, and explicit error-handling requirements.

## DropdownMenu progress

- Added a sheen-owned discriminated menu tree with nested actions, controlled checkbox/radio values, separators, and display-only Kbd hints. Stable sibling IDs preserve focused DOM through label/value/callback refreshes; validation rejects ambiguous IDs, empty labels, and cyclic trees while closed. Each layer uses its scoped portal, effective locale/direction, provider ordering, inert/hidden exit state, and token-based transient motion.
- Three SSR tests cover native triggers and whole-tree validation. Six behavior tests cover action ownership, disabled skipping, Home/End/typeahead, controlled rejection, outside focus, refreshed-node identity, nested RTL keyboard navigation, one-layer Escape, and pre-hydration click/Enter/ArrowDown replay without trigger replacement or duplicate actions. After fixing deferred autofocus, the nested keyboard scenario passed ten repeated runs. Its regression explicitly flushes pending autofocus after navigation and verifies that focus stays on the chosen item.
- Live tests exposed upstream defects: an infinite locked-document scroll traversal, browser-locale-driven direction instead of explicit scoped direction, menu Escape ignoring preventDefault, and deferred autofocus resetting already-established item focus. Version-pinned compatibility patches fix the underlying paths; both JSX and compiled runtime variants are covered by the patch files. ThemeProvider/ThemeScope now provide Kobalte their effective locale and independent direction. Consumer/scaffold qualification must carry or supersede these patches, as documented in dependency-patches.md.
- The complete package check and all 74 Chromium tests passed after the final autofocus and RTL glyph corrections, without snapshot-update mode. Two reviewed screenshots cover dark checked/shortcut states and light RTL nested layers, with bounded geometry, reduced motion, and text/focus contrast checks. The grouped DropdownMenu task is checked; broader all-theme/state, axe, WebKit/Firefox, and release qualification remain open.

## Tooltip and Popover progress

- Added Tooltip as a native Button-compatible action with noninteractive supporting text, optional Kbd shortcut display, merged descriptive associations, native form/disabled/loading behavior, focus/hover activation, hoverable content, and Escape dismissal. Mount reconciles pre-hydration focus/hover without replacing the trigger. Automatic shortcut association remains part of the unfinished shortcut-registry work.
- Added titled, nonmodal Popover with controlled/uncontrolled ownership, optional description, app-owned body, scoped close label, viewport positioning, raised-surface focus offsets, and retained inputs/drafts on refresh. Both layers use scoped portal targets, provider ordering, top-layer accessibility protection, and fast enter/exit motion with zero-duration reduced motion.
- Two real SSR tests cover native triggers and absence of premature overlay markup. Five behavior tests pass for tooltip description/shortcut/hover/action behavior, popover ownership and retained drafts, nested modal scope and one-layer Escape, pre-hydration Tooltip focus, and queued Popover clicks retaining native identity. Outside Popover dismissal preserves deliberate external focus.
- Three reviewed screenshots cover dark Tooltip/Popover and a light RTL Popover inside a modal; viewport and scoped text/focus contrast checks pass. The full package check and all 67 Chromium tests pass without snapshot-update mode. The grouped task is checked; broader all-theme/state, axe, cross-browser, and release qualification remain open.

## Dialog-family progress

- Expanded Dialog with controlled/uncontrolled ownership, optional trigger, explicit initial/return focus targets, dismissibility control, descriptions, and app-owned footer actions. The shared modal frame owns header/body/footer spacing, raised-surface focus offset, bounded overflow, RTL positioning, and transient/reduced motion. Exiting content becomes inert and accessibility-hidden immediately, retaining its last layer position during the fade.
- Added AlertDialog with required explanation, outside-press protection, and safe initial close focus. ConfirmDialog supplies localized safe/affirmative actions; createConfirm returns an owner-scoped boolean decision promise without performing app data operations. Abort/disposal resolves false, overlap rejects without replacing the prompt, and closing content remains mounted for its exit animation.
- Four new behavior tests cover retained input identity/drafts through description refresh, controlled close rejection, explicit focus return, locked dismissal, nested dismissal, alert keyboard trapping and RTL geometry, confirmation acceptance/cancellation/abort/overlap/disposal, and reduced motion. Existing delayed-hydration trigger and nested scoped Select/dialog regressions pass in the full run.
- Real SSR/unit tests verify closed native trigger markup, triggerless ownership, and confirmation lifecycle settlement. Initially open portal content remains client-mounted when its theme target becomes available, as documented in component-authoring.md; this is not server-rendered modal-content proof.
- Three reviewed full-viewport screenshots cover focused dark Dialog and light/RTL AlertDialog/ConfirmDialog. Visual review drove spacing between fields and actions. The complete package check and all 61 Chromium tests pass without snapshot-update mode. The grouped dialog task is checked; broader theme/state, axe, cross-browser, and release gates remain unfinished.
- Confirmation's compiling metadata example exposed that extraction imported only components. The validator now imports runtime exports, allowing public helpers in examples while excluding type-only declarations. All 49 extracted examples typecheck.

## Select progress

- Added Select with a sheen-owned single-string-value API, explicit controlled null selection, native/external form participation, required-validation focus redirection, disabled/read-only behavior, descriptions/errors, localized empty prompt, and metadata/demo/SSR tests.
- Explicit server label/value associations avoid effect-only accessibility registration. The popup mounts in the nearest theme portal and participates in provider layering. A nested-dialog browser failure exposed hide-outside processing hiding option labels; the Kobalte top-layer marker fixes this, with scoped color/direction and single-layer Escape assertions.
- Option collection replacement initially remounted nodes and dismissed the popup. Stable value-keyed rendering through the primitive's custom-children path now retains DOM identity; reconciliation is not treated as user intent. Removal uses enabled-neighbor/empty-listbox focus fallback, and restoring an unavailable selected option restores its presentation without changing owner state.
- Eight behavior tests pass, covering keyboard/typeahead/Home/End, repeated selection, controlled rejection, read-only inspection, native reset/cancellation/required/external forms, transient/reduced motion, scoped dialogs, and delayed hydration with click/ArrowDown/Enter/Space/typeahead. Exiting content becomes inert and hidden from accessibility immediately while its visual fade finishes.
- The full concurrent suite exposed navigation keys arriving before hydration. Exported createKeyboardHydrationScript supplements Solid's click/input bootstrap with Select arrow/typeahead replay, without duplicating native Enter/Space clicks. Loupe emits it; scaffold and dogfood consumers must carry it as documented in docs/hydration.md. This is not external-consumer qualification.
- Five reviewed visual baselines cover dark, light, compact, high-contrast/spacious, and RTL controls plus open listboxes. Enabled option text and focused popup contrast pass in this bounded matrix. The complete package check and all 56 Chromium tests pass without snapshot-update mode. The Select TODO is checked; broader axe/all-theme component-state/cross-browser release gates remain open.

## Checkbox progress

- Added Checkbox with a sheen-owned typed API over the existing Kobalte dependency, mandatory visible label, description/error associations, native form values, controlled/uncontrolled ownership, disabled/read-only behavior, app-owned mixed presentation, and token-based focus/selection styling. Metadata, demo, SSR tests, and production fixture build pass.
- Real browser tests exposed generated internal IDs leaking into unnamed form fields and canceled resets still changing state. The wrapper omits unnamed values from submission and owns reset after the native default action. Uncontrolled defaults reset with change notification; controlled values remain app-owned; canceled resets preserve state. Pending reset work is disposed with its owner.
- Delayed-JS testing exposed live native checkedness disagreeing with the hydrated visual state. The wrapper reconciles the live input on mount, adopting uncontrolled choices and proposing controlled changes to accepting/rejecting app owners. CSS responds to native checkedness before hydration, with a persistently mounted decorative indicator. Tests also cover DOM identity, focus, associations, keyboard activation/tab order, mixed resolution, form values, and RTL placement.
- Both targeted Chromium tests pass after the shared boolean-state extraction for Switch. CheckboxGroup and RadioGroup are now implemented below. Broader release-browser qualification remains open.

## Switch progress

- Added Switch over the existing Kobalte primitive with a sheen-owned API, mandatory stable label, description/error associations, native form participation, controlled/uncontrolled ownership, and disabled/read-only semantics. It has no mixed state. Metadata/demo, SSR tests, and production fixtures pass.
- Checkbox and Switch share their native-input ownership helper, including pre-hydration edits, controlled acceptance/rejection, uncanceled reset defaults, canceled-reset preservation, unnamed-field exclusion, and owner cleanup. Both components' browser regressions pass after extraction.
- Switch browser tests verify Space/tab behavior, disabled skipping, read-only preservation, form serialization, reset cancellation, pre-JS thumb response, delayed hydration retaining native identity/focus, dynamic errors, and RTL inline-end thumb placement.
- Five reviewed choice-control screenshots cover dark, light, compact, high-contrast/spacious, and RTL states. Computed boundary, state-indicator, and focus contrast checks pass in those configurations. Visual review exposed compact focus touching the label; the inline gap now uses the next role token, and focused-label clearance is asserted geometrically. This bounded matrix does not replace all-theme/accent or cross-browser release qualification.

## Checkbox-group progress

- Added CheckboxGroup using native fieldset/legend semantics and Checkbox options keyed by unique string value. It supports controlled/uncontrolled selection, descriptions/group errors, group/option disabled states, read-only controls, native repeated-name submission, external forms, and canceled-reset preservation. Selection callbacks receive fresh deduplicated arrays; unavailable values remain owner state without being submitted. SSR tests reject duplicate identities and cover controlled empty selection.
- External form support explicitly associates each child input, because fieldset association alone is not inherited by descendants. Checkbox and Switch now expose the same native form seam. Browser coverage exercises external group and switch submission/reset alongside ordinary groups.
- A focused-option reorder regression exposed browser blur despite retained DOM identity. The group restores surviving focus after reconciliation without overriding deliberate external focus. Removal falls back to the next enabled option, then the previous, then the programmatically focusable group. Restoration is disposal-guarded and does not scroll.
- The five reviewed choice-control visual baselines include grouped choices, legends, descriptions, errors, and disabled options. Their computed boundary/state/focus contrast checks pass. Production fixtures and the complete package check pass.
- All three group behavior tests pass in the full 43-test browser run. Removal testing also required an always-visible ring on the programmatic group fallback after mouse interaction. The full run exposed an older Skeleton theme assertion sampling before the update settled; it now uses a retrying CSS assertion without delaying the action until hydration. Lint and typechecks pass after those final changes.

## Radio-group progress

- Added RadioGroup with required visible label and nonempty native group name, stable option identities, controlled/uncontrolled selection, explicit null selection, required validation, descriptions/errors, disabled/read-only states, horizontal/vertical orientation, and external form support. Metadata, demo, and real SSR tests pass.
- Delayed-JS testing exposed missing accessible names before Kobalte's effect-based registration. Explicit stable input/label/description IDs now provide complete server associations. Native selection is visible before hydration; mount reconciles accepting/rejecting controlled owners and uncontrolled choices without replacing inputs or losing focus.
- Four Chromium tests cover arrow wrapping/disabled skipping, tab entry, three-enabled-option RTL navigation, native exclusivity, required validation, form serialization, canceled/uncanceled reset, controlled rejection, and delayed hydration. Option refresh/removal uses the shared CheckboxGroup focus helper, preserving surviving nodes without stealing external focus or changing selection.
- Five visually reviewed screenshots cover dark, light, compact, high-contrast/spacious, and RTL states. Each configuration includes 28 choice controls; computed checks validate boundaries, visible indicators, and focus contrast/label clearance for every enabled control. The complete package check and all 47 Chromium tests pass without snapshot-update mode.
- The grouped Checkbox/CheckboxGroup/RadioGroup/Switch task is checked. All-theme component-state coverage, axe, WebKit/Firefox, and release qualification remain separate unfinished gates.

## Field progress

- Added Field with mandatory visible label, stable reactive control bindings, description/error associations, and native required/disabled forwarding. Input now composes Field and merges caller-provided descriptive IDs rather than nesting a second label. Validation and announcements remain app-owned.
- Metadata/demo and SSR tests pass. The `/forms` Chromium fixture verifies hydration identity, label activation, disabled semantics, dynamic error associations, and focused draft retention. Production fixture builds and the full 35-test browser run pass.
- The grouped Field/InputGroup/Textarea/SearchInput TODO is checked. Five visually reviewed full-sample baselines cover dark, light, compact, high-contrast/spacious, and RTL configurations, including errors, focus, addons, search, disabled/read-only fields, and textareas. Viewport assertions prevent clipped captures. Computed placeholder colors pass AA, and AAA in the contrast configuration. Cross-browser, axe, and broader release gates remain separate unchecked work.

## Textarea progress

- Added labeled Textarea with Field validation/description associations and optional frame-batched autosizing. Measurements respond to edits, controlled values, width/font/theme changes, and form reset; max-block-size caps growth with native scrolling. Observers, font/reset listeners, and scheduled frames are owner-cleaned. Autosize is off by default and rows reserves server geometry.
- Delayed-JS testing exposed an invalid SSR value attribute, which browsers ignore for textarea contents. Initial values now render as escaped text content, including protection against HTML's leading-newline removal. Client updates use the value property and reuse the server control.
- Chromium coverage checks initial text before hydration, DOM identity, growth/capping/shrinkage, controlled replacement, autosize toggling, width wrapping, draft retention across density changes, and reset geometry. The complete local package check passes with the production textarea fixture. These tests pass in the full browser run alongside the reviewed form visual matrix.

## Search-input progress

- Added native SearchInput with required label, controlled/uncontrolled query ownership, localized explicit clear action, and Field associations. Empty/disabled/read-only states disable clearing. Clear restores input focus; the component does not fetch, debounce, or own results. The default clear-search message is independently overridable through theme messages.
- SSR tests cover controlled precedence, localization, and clear-action disablement. Five form browser tests pass in the full suite, covering hydration reuse, native input callbacks, controlled acceptance/rejection, clear behavior/focus, InputGroup regressions, native reset, and IME composition. The production fixtures and reviewed visual matrix pass.
- Trusted Chromium IME events reproduced controlled-value rejection overwriting active composition. SearchInput now retains that draft locally until composition ends and disables clearing during composition. The regression covers rejecting/accepting controlled parents and uncontrolled input. Other browser and OS IME qualification remains a release obligation.
- Native reset testing first reproduced an empty DOM input with a stale enabled clear action. SearchInput now restores its captured uncontrolled default and emits a changed value after the native reset action; controlled queries retain app ownership. The regression verifies empty/nonempty defaults, callback state, controlled preservation, and canceled reset. A microtask proved too early for the browser's reset default action, so synchronization uses a cleanup-owned next-task callback. Pending callbacks and the document listener are removed on owner disposal.

## Input-group progress

- Added InputGroup with mandatory label, inherited description/error semantics, logical start/end content, and independently owned native actions. Adornments are resolved once with Solid's children helper. A real delayed-hydration test exposed duplicate slot evaluation before this correction; an SSR render-count regression now guards it.
- Form browser tests pass after the fix, covering grouped input hydration identity, keyboard order, dynamic unit descriptions, RTL placement, and draft retention. These now pass in the full 35-test browser run, and the production fixture and bounded visual matrix are qualified locally.

## Description-list progress

- Added native DescriptionList/DescriptionTerm/DescriptionDetails exports, exhaustive metadata, and semantic demos. Stacked and column layouts use logical spacing, wrap unbroken values, and support tabular figures without owning formatting.
- Two targeted Chromium tests pass, covering RTL column geometry, hidden content, narrow-viewport wrapping, keyboard order, retained input drafts during theme/layout changes, and delayed hydration preserving all server term/detail/input identities and list geometry. Two complete screenshot baselines were visually inspected and passed without snapshot-update mode. This is separate from the earlier full-suite run.
- The grouped loading/data-display implementation task is checked after combined visual qualification and the full browser rerun. DescriptionList and styled Table do not implement DataTable selection, pagination, or request handling.

## Styled table progress

- Added Table, TableCaption, TableHead, TableBody, TableFoot, TableRow, TableHeaderCell, and TableCell with native props/semantics, numeric alignment/tabular figures, optional stripes, and stripe-local focus offsets. All eight exports have metadata and contextual demos. Header cells default to column scope; row headers explicitly select row scope.
- SSR tests cover nesting, associations, native spanning/hidden attributes, and absence of grid roles or row tab stops. A real Chromium test passes for delayed hydration identity, keyboard order through cell inputs, stripe offsets, compact sizing, RTL, and retained drafts. The latest full build includes table, loading, and combined visual routes. Five reviewed full-sample screenshots cover dark, light, compact, high-contrast/spacious, and RTL configurations; viewport assertions prevent clipped captures.

## Spinner progress

- Added Spinner with localized status text, three semantic icon sizes, and a decorative mode suppressing the status role and announcement text. Rotation uses the slow motion token; reduced-motion tokens stop it without hiding the loading status. Metadata, demo, and SSR tests are present.
- Four Chromium loading tests pass, including Spinner DOM retention through delayed hydration, size tokens, decorative semantics, scoped/OS reduced motion, and a two-frame transform stability check under reduced motion. They also pass in the full suite alongside the combined visual matrix.

## Empty-state progress

- Added EmptyState with localized empty/no-results headings, an optional explanation, and app-owned action children. Each region has a stable visible name, without automatic live announcements or focus movement. Metadata, demo, and SSR tests cover localized defaults, distinct IDs, explicit naming, native props, and action markup.
- The `/loading` browser fixture exercises no-results to empty transitions and delayed hydration of the named region alongside Skeleton. Production build and bounded visual qualification pass.

## Skeleton progress

- Added a static, aria-hidden, inert Skeleton with text/rectangle/circle shapes, token geometry, metadata, and a demo. Native logical sizing is supported. It never wraps existing content or owns a loading timer.
- Two real Chromium tests pass for token geometry, native hidden state, inert tab skipping, theme updates without replacement, no animation with either motion preference, and delayed hydration retaining server DOM and bounds. The `/loading` fixture builds and the combined visual matrix passes.

## Hydration replay correction

- The expanded full run initially exposed lost clicks before hydration in Save and dialog controls. A deterministic delayed-script test reproduced a queued click being consumed without a handler call. Solid 1.9.15's delegated replay handler used an empty post-dispatch composed path.
- Applied a version-pinned pnpm patch to the four browser runtime builds, falling back to existing event-target traversal when the composed path is empty. Offline installation applied the patch and recorded its lockfile hash. The regression now verifies queued Save/dialog actions and a subsequent live click, and the full 27-test Chromium run passes. No hydration waits were added to hide the lost interactions.
- See docs/hydration.md for evidence and consumer obligations. External applications do not inherit workspace patches automatically; scaffolds/migrations must carry the patch or qualify an upstream fix. Hosted CI, WebKit, and external consumer replay remain unverified.

## Agent-context progress

- Added a validated manifest-to-context generator. `pnpm manifest` now writes compact and full context plus a vendorable skill only after metadata/example validation. A focused real unit test covers inherited-prop exclusion, type normalization, complete guidance, and budget rejection. The generated outputs and deterministic 45,000-byte compact ceiling are wired into the normal manifest and CI path, completing the generation checklist item; pinned-model token qualification remains part of the separate assay gate.
- The compact context currently covers all 144 exported components in 43,462 UTF-8 bytes against a deterministic 45,000-byte ceiling. The compact syntax preserves every one-line summary, authored prop signature, and canonical example while aliasing repeated type words and removing cosmetic example whitespace. This is a model-independent proxy, not exact proof of the specification's under-15k-token target; exact qualification remains with the pinned M5 assay model/tokenizer.
- Added a 68-line repository `AGENTS.md` and consumer guidance for Solid reactivity, public wrapper seams, token roles, dark-default theming, scoped hydration, retained refresh content, and paginated versus bounded continuous tables. The skill artifact carries the same core constraints. CLI sync/scaffolds, lint rules, and the assay remain unfinished.

## CLI scaffold progress

- Added the complete `sheen new app` plan. Its nineteen files configure current SolidStart 2 through Vite, the public Solid Router adapter, fixed AppShell, contextual ThemeProvider/ShortcutProvider/Toaster owners, dark-default client hydration, all six themes and twelve accents, the prepaint and keyboard bootstraps, recommended lint, starter route, vendored agent context, and the pinned Nix environment. The scaffold carries byte-exact Solid/Kobalte compatibility patches and activates them through pnpm workspace configuration.
- A real isolated test writes the plan, typechecks every generated TypeScript source against the installed contracts, and completes a SolidStart production build. The built CLI artifact independently loads all nineteen bundled scaffold assets. It does not install the future `^1.0.0` package range from a registry, so publication remains a separate M6 gate.
- Added the dependency-free `@gemologic/sheen-cli` package and guarded plan engine. Theme scaffolds derive validated light/dark definitions from Obsidian with dark as the default; component scaffolds generate implementation, exhaustive metadata, demo, and SSR-test files together. The generated sources typecheck against the real workspace modules in a ts-morph project, so the theme/component scaffold TODO is complete.
- Live scaffolds preflight every path and refuse the whole operation if any target exists. Dry runs do not write; forced replacements call the preview sink with all diffs before the first write. Path containment and duplicate-plan validation prevent an output from escaping its selected root.
- Added vendored-skill packaging and `sync-skill`, defaulting by Sheen convention to `.agents/skills/sheen` with an explicit relative `--target` override. Initial install, no-op, dry-run, containment, and fail-closed non-Git edit cases pass. The implementation uses only read-only Git queries, requires changed existing targets to be tracked and clean, and offers no force bypass. The combined sync-skill TODO remains open until clean/dirty tracked paths run in a temporary real repository without violating this checkout's no-Git-mutation policy.
- Added read-only `doctor` configuration/version checks, exact suppression counting, and typed runtime ShortcutBinding snapshot ingestion. Two focused tests compare the complete fixture tree before/after, exercise the rendered binding table, and prove actionable status 1 diagnostics for version drift, bootstrap gaps, suppression excess, and invalid snapshots. The doctor TODO is complete; lint-rule coverage remains separate.
- Added `sheen manifest` forwarding to the selected repository's real pnpm script, with a real temporary-package execution test and preserved failure output.

## Agent-assay fixture progress

- Added the frozen `sheen-agent-v1` suite with twenty prompts and per-prompt rubrics spanning shell, themes, hydration, refresh, paginated and continuous tables, forms, overlays, charts, streaming, RTL, and private themes. Universal criteria cover public imports, manifest-valid props, Solid reactivity, semantic tokens, native semantics, hydration, and refresh continuity.
- Added executable suite/report validation and aggregate scoring. Any hallucinated prop or failed rubric item is a hard failure; aggregate lint density must be at most one per 100 lines and Sheen interaction coverage at least 80%. Relative regression checks enforce the accepted 5% baseline rule. Three focused tests pass. Diagnostic pinned-model runs are recorded in Current qualification; no complete twenty-output baseline exists, so the baseline and context-change gate TODOs remain open.

## Lint-plugin progress

- Added `eslint-plugin-sheen` using the existing ESLint 10.10 runtime and the official flat plugin shape. Its recommended config enables all seventeen specified JavaScript/TypeScript rules. Correctness boundaries are errors; dynamic icon names, typed unsafe seams, and layout substitutions are warnings. Theme/token authoring directories, owning wrapper packages, layout implementation files, and named transient layers are explicit exceptions.
- Eighty-two real RuleTester cases cover literals, template quasis, object-style values, logical alternatives, semantic token references, theme boundaries, public dependency seams, Solid prop reactivity, class forwarding, the chart token bridge, fixed-shell scrolling, mount motion, icon semantics, and layout composition. Separate tests prove the lint icon list exactly matches the runtime registry and every rule is present at the intended recommended severity. Package build, workspace/Loupe typechecks, and repository lint pass.
- Static JavaScript/TypeScript inspection cannot prove computed runtime class strings or standalone CSS. Stylelint remains the CSS enforcement boundary, and its unfinished status is stated rather than folded into this completed ESLint rule family.
- Added `sheen manifest` forwarding to the selected repository's real pnpm script and a read-only `sheen doctor`. Doctor verifies runtime dependency placement/version alignment, hydration and stylesheet bootstraps, the suppression ceiling, and optional complete runtime `ShortcutBinding` snapshots. Tests compare the entire fixture tree before/after a passing audit and exercise actionable multi-error output with exit status 1, completing the doctor version/config/snapshot TODO.

## Font packaging progress

- Added a separately importable `@gemologic/sheen-tokens/fonts.css` entry with unmodified IBM Plex Sans 400/600 and IBM Plex Mono 400 WOFF2 assets from a pinned official IBM revision. The published package retains the upstream paths, SHA-256 digests, and SIL Open Font License 1.1. Unit tests verify all three binary identities and the package-consumer test follows every stylesheet URL to a real installed WOFF2 file.
- The font display policy is `optional`: fast same-origin loads use Plex for the initial render, while a deliberately stalled response keeps fallback geometry rather than swapping component text after paint. Chromium coverage verifies all three faces in the normal path and unchanged geometry after a delayed font response. The two affected typography baselines were visually reviewed and regenerated for the actual Plex rendering, then passed without update mode.
- Numeric Text, description, table, pagination, navigation-count, and status roles use tabular figures; explicit OpenType `tnum` activation accompanies the CSS shorthand. Cross-platform font rasterization remains part of release-browser qualification.

## AppShell hydration-anchor progress

- Fixed the reproducible desktop delayed-hydration drift from 400px to 399px by assigning exact scroll anchoring to the constrained AppShell pane at desktop widths. Phone presentation keeps native document anchoring because the document, not the inert ScrollArea viewport, owns its scroll coordinate.
- The same investigation exposed a separate 30px phone shift: IBM Plex became available only when the delayed client graph activated, narrowed the raw fixture header by one wrapped line, and changed accepted document geometry. The token package now exposes public bundler-resolvable WOFF2 entries; Loupe and `sheen new app` preload all three configured faces before application assets. The optional font-display policy remains unchanged.
- Closed the M1 0.1.0 qualification gate. A clean-server Chromium run passes all 537 browser cases with four fixed workers, following the complete 140-file/584-case static, unit, and SSR gate plus standalone built/source consumer-browser checks. The harness now accepts an isolated `SHEEN_BROWSER_PORT`, clipboard permission follows that configured origin, and stale assertions reflect the compact saved-view dropdown, current single-property motion CSS, and the sidebar scroll container. Separator now stamps the orientation data consumed by its logical-size CSS, and SplitLayout panel IDs are unique across multiple server-rendered instances. The full IBM Plex-affected visual set was reviewed, regenerated, and passed again without update mode. This qualifies a release candidate only; no version or publication state changed, and WebKit/manual assistive-technology evidence remains tracked separately.
- Three repeated delayed-hydration runs pass for both desktop and phone. The complete 26-case Chromium shell/restoration matrix passes, including responsive projection, independent history coordinates, short/growing data, RTL, input interruption, sidebar projection, accepted-node identity, and exact offsets. Workspace and Loupe typechecks, lint on changed TypeScript, and the real generated-app scaffold build also pass. WebKit and hosted CI remain release-level proof obligations.

## DataTable presentation progress

- Added one shared DataTable engine with explicit `integrated` and `framed` presentations plus contextual omission. Standalone tables form one joined framed surface; DataTablePage supplies an edge-to-edge integrated context, while an explicit child presentation always wins. No toolbar, row, editor, pagination, or mobile implementation is cloned.
- Compact density now applies coherently to toolbar controls, headers, fixed and measured rows, selection controls, live editors, footers, pagination, and phone cards. Integrated tables fill the available DataTablePage pane; framed tables retain a bounded initial viewport. Mobile cards deliberately shed the desktop outer frame and retain their own surface boundaries.
- Forty-one Chromium cases pass across continuous and numbered pagination, integrated and framed chrome, compact density, variable-height rows, column layout, grouping, hierarchy, keyboard selection, hydration, refresh, edits, phone cards, and reviewed dark visual baselines. The 150-component manifest, 154 extracted examples, generated 44,982-byte compact context, workspace typecheck, changed-file lint, and isolated consumer/bundle gates pass. WebKit and hosted CI remain release-level proof obligations.

## DataTable search progress

- Added explicit accessor-column search with either the accessor value or a typed string/number projection. Column definition validation rejects search on display/action, initially hidden, or sensitive columns and rejects invalid projected values. DataTable search is enabled only when at least one eligible column exists, has localized accessible defaults, supports bounded debounce and an optional registered shortcut, and can be disabled explicitly.
- Search drafts remain separate from accepted `TableState`. IME composition does not schedule work, accepted changes reset pagination, and client and server paths share URL, saved-view, export, and inverted-selection state. Server requests use the existing AbortSignal and monotonic response token. Accepted rows, focus, selection, and DOM identity survive pending work; failed requests retain both accepted content and the retryable draft while clearing busy state. Delayed hydration adopts native or autofilled search text rather than erasing it.
- Twenty-seven focused state, saved-view, export, and selection unit tests plus nine Chromium search/URL tests pass. The wider 62-case Chromium table matrix, workspace and Loupe typechecks, changed-file lint, package builds, exhaustive 150-component manifest with 154 compiling examples, and isolated consumer/bundle gates also pass. Chrome DevTools inspection confirmed the compact integrated geometry and accessible search/table/pagination tree; Lighthouse reported 98, with its sole failure caused by the standalone Loupe fixture lacking a document `main` landmark rather than DataTable markup. WebKit, manual assistive-technology qualification, and the application-level landmark remain open release gates.
- The retained-search tests exposed and corrected two adjacent defects: ContextMenu now preserves its native trigger when delayed items become available, and no-results/empty table cells can grow above a sticky footer without clipping their action.

## DataTable capability-toolbar progress

- Replaced the flat wrapping controls with one internal, named capability toolbar. Search and active FilterBar chips share the leading query region; accepted locale-formatted result counts, all-matching selection, available export formats, the retained download action, and column controls are derived from one accepted DataTable owner. Page actions remain separately owned and named by DataTablePage, and neither layer accepts the other layer's controls.
- Secondary controls use one action/menu model for their wide direct presentation and narrow overflow presentation. A component-width container query switches to a single More actions menu, including nested Export and Columns models, without client measurement. The server and hydrated first frames therefore agree. A delayed-script Chromium regression proves exact toolbar geometry plus retained toolbar and row nodes before and after hydration, then crosses the container boundary without replacement.
- Twenty-eight focused SSR tests, 37 changed-area functional Chromium tests, and four visually reviewed Chromium baselines pass. Workspace typecheck, changed-file lint, UI/table production builds, isolated consumer/tree-shaking checks, and the exhaustive 150-component/154-example manifest pass. DataTable measures 21.93KB gzip after the internal owner. WebKit and manual assistive-technology qualification remain release gates.

## DataTable saved-view chrome progress

- Replaced the expanded saved-view form with one compact current-view switcher. Its menu restores a selected snapshot or switches directly among saved views, while focused save and management dialogs own create, rename, restore, and confirmed delete workflows. DataTablePage no longer accepts external query controls, so search, filters, results, columns, and export cannot be duplicated above the table-owned toolbar.
- Saved-view publication and app selection now update atomically. Renames preserve the stored table state, failed create and delete operations retain the accepted table and retry the exact operation, and successful retries complete the corresponding selection transition. The page, table, rows, focus, drafts, and accepted data stay mounted throughout.
- Eleven focused unit tests and sixteen Chromium page/accessibility cases pass, including two-record switching, error recovery through the real HTTP adapter, delayed hydration with retained trigger/row identity and focus, and automated WCAG A/AA checks for the open dialog. The visually reviewed compact dark baseline passes after regeneration. UI/pattern production builds, Loupe production/type checks, changed-file lint, isolated consumers, and the exhaustive 150-component/154-example manifest pass at 44,986 bytes. Chrome DevTools reports a clean semantic tree and Lighthouse accessibility 100 for the open save dialog; WebKit and manual assistive-technology qualification remain release gates.

## DataTable interaction-polish progress

- Sortable headers now expose visible direction and numbered multi-sort priority while reserving `aria-sort` for the primary column. Separate accessible descriptions retain every secondary direction and priority. Resize separators report pixel values, reorder handles use a real grip with keyboard-visible affordance, and pinned edges gain a control boundary and shadow while their cells inherit row-state fills.
- Rest, hover, keyboard focus, selection, selected interaction, and retained previous-query rows have distinct opaque states across normal and pinned cells. Failed delegated queries retain the warning-tinted Previous results presentation and keep query and row operations guarded until retry accepts the requested state. Empty search results use an accurate Clear search and filters label. The bounded selection bar uses a raised themed surface, finishes its exit animation before unmounting, and bounds wide actions without changing table geometry.
- The polish pass found two cross-theme contrast defects: the selection bar had treated the translucent backdrop token as a content surface, and button text interpolated through a low-contrast color during an otherwise atomic theme swap. The bar now uses the raised-surface token and buttons transition only their hover background. An immediate post-switch assertion verifies every light compact selection-bar label at 4.5:1 or higher.
- Twenty-six focused SSR/unit cases and forty-six Chromium table, page, hydration, visual, and automated WCAG cases pass. Reviewed baselines cover obsidian dark comfortable, paper light compact, and contrast light spacious RTL. UI, table, and Loupe production builds, workspace and Loupe typechecks, changed-file lint, the 150-component/154-example manifest at 44,986 bytes, and isolated consumer/tree-shaking gates pass; DataTable is 22.38KB gzip. Chrome DevTools exposes both sort descriptions, pixel resize values, no console warnings/errors, and Lighthouse accessibility and best-practices scores of 100. WebKit and manual assistive-technology qualification remain release gates.

## DataTable assistive-structure progress

- Virtualized flat tables now report their complete accepted row count and a one-based logical index for every rendered header, data, and footer row. Numbered pages retain the global query offset rather than restarting at row 2. Grouped and hierarchical treegrids use the ARIA unknown-total sentinel while preserving monotonic indices for the expanded rows they expose.
- Tables report the full declared column count, including selection. Every rendered header, body, state, and footer cell has a logical column index, so a hidden column leaves the required explicit gap. Selectable data rows now expose `aria-selected` without replacing native flat-table semantics or their labeled checkbox controls.
- A dedicated real-browser suite drives search, filtering, sorting, hidden columns, virtualized End navigation, row selection, invalid inline editing, delegated search, and server pagination with keyboard input. It checks atomic live results, accessible sort descriptions, row/column coordinates, referenced edit errors, retained accepted-page announcements, and focus/DOM identity during actual delayed requests. Twenty-five focused SSR cases and the full fifty-nine-case changed-area Chromium matrix pass, including the four new accessibility cases and the two reviewed table-chrome baselines that had become stale. Chrome DevTools confirms the live structure, reports no console warnings or errors, and gives the snapshot an accessibility score of 100. Production UI/table/Loupe builds, workspace and Loupe typechecks, changed-file lint, the exhaustive 150-component/154-example manifest, and isolated consumer/bundle gates pass. WebKit and manual screen-reader sessions remain release-level proof obligations.

## Character-shortcut accessibility progress

- Closed the WCAG 2.1.4 mechanism requirement using its explicit turn-off option. `ShortcutProvider.characterShortcuts` is wired to a visible native Switch in the fixture; disabling it suppresses single printable bindings and all-character sequences, including Shift-generated punctuation, without suppressing Ctrl/Meta/Alt or non-character bindings. The visible generated help remains available and identifies disabled entries.
- Unit classification covers letters, punctuation, Unicode characters, emoji, shifted keys, sequences, mixed modified sequences, and preference changes during a pending prefix. Real Chromium coverage operates the setting by keyboard, verifies disabled native key dispatch, preserves a pre-hydration preference edit and focus, and re-enables the question-mark action. This satisfies the W3C G217 mechanism and avoids F99 by construction. No named speech-recognition product or non-Chromium engine was executed.

## Semantic-icon progress

- Added 42 semantic names with generated Radix and Phosphor data, token sizes, muted/inherited color roles, decorative defaults, and runtime validation for labeled meaningful icons. Five bundled themes select Radix and Vellum selects Phosphor; provider, scope, portal, prepaint, and cookie attributes share that metadata.
- The Vite integration replaces literal Icon markers with virtual per-icon modules and rejects unknown names. Explicit per-icon exports use split generated data modules, while DynamicIcon is the deliberate full-registry path. Real isolated Vite consumers measure 2.8KB minified literal/static output versus 38.9KB dynamic output and prove unrelated glyph removal.
- A CSS-specificity regression initially hid both SVGs for the default Radix set. Equal-specificity fallback and selected-set rules now preserve the one-set fallback and show exactly one selected glyph. Fourteen Chromium tests pass across the icon route, generated Icon docs, client/cookie themes, delayed JavaScript, nested scopes, and scoped portals. The live-switch test retains the original node and observes one Phosphor glyph in all 13 sampled frames.

## Cross-system laboratory progress

- Added `/lab` with URL-backed theme, accent, mode, density, radius, motion, direction, locale, width, and height axes. Theme preferences also update the root provider, so its client-hydration storage contract owns reload persistence. `/lab-preview` applies the URL state through a controllable scope and formats its numeric sample with the explicit scoped locale.
- The iframe source is stable after its initial navigation. Later axis changes use validated same-origin messages, update the existing scope, and retain component owners and uncontrolled drafts. Browser coverage samples twenty consecutive animation frames after changes and requires the marked preview to remain connected, nonzero, and opaque.
- The resizable viewport uses content-box dimensions because the values describe the iframe's actual content viewport. A `ScrollArea` contains device widths larger than Loupe without shrinking them or introducing document scroll. The observer reads current client geometry in its queued frame; it never republishes a stale observer entry. Local state updates before URL synchronization, preventing rapid width/height edits from racing navigation.
- Three Chromium cases pass for all live axes, root storage and reload restoration, exact keyboard-entered dimensions without iframe reload, and direct delayed hydration that retains marked server nodes and a draft typed before JavaScript. Unit parsing, repository lint/typechecks, and the Loupe production build also pass. Native pointer dragging, WebKit, and release-wide first-paint qualification remain separate gates.
- Added `/comparisons` as an independent A/B workbench. Both panes expose every bundled theme and accent plus mode, density, and exact 320–1600px width controls. Each configuration drives a separate component ThemeScope and a stable full-layout iframe; swap preserves the existing owners instead of exchanging DOM subtrees. Prefixed URL parsing is validated per pane. A focused Chromium case verifies default narrow/wide viewports, live scope/frame changes, root isolation, URL state, exact dimensions, and retained component/iframe drafts through changes and swap. The M4 comparison checklist line is complete; gallery compositions and broader matrices remain separate.
- Added URL-backed baseline-grid, spacing-outline, persistent focus-ring, forced visual state, color-vision, reduced-vision blur, and performance-meter controls. Color filters use the same Machado severity-one linear-light matrices as palette qualification plus a luminance-based achromatic preview. The diagnostic meter reports FPS, p99 frame interval, and Long Tasks per window rather than treating mean FPS as a gate. Live Chromium coverage verifies applied computed styles and retained draft/node identity; direct delayed hydration proves the selected debug state and filtered server subtree survive handoff. The debug-overlay checklist line is complete, but calibrated benchmark gates remain authoritative.

## Layout-gallery progress

- Added a gallery index and an operations dashboard built from public StatGroup, Sparkline, Card, Table, Badge, Input, EmptyState, Skeleton, and ErrorState contracts. Ready, empty, loading, server-error, and permission-denied states are URL-selectable. A real delayed background refresh keeps the marked accepted composition and uncontrolled operator draft populated for twenty sampled frames before publishing the new revision.
- Extended the existing full DataTablePage scenario with deterministic `rows=100000` and `rows=empty` modes. The 100k dataset remains behind an explicit query and is created lazily, so ordinary table fixtures do not pay the cost. Gallery links expose the same filter, selection/action, export, column-control, mobile, view-adapter, status, refresh, loading, error, and permission contracts in explicit continuous or numbered-pagination presentation.
- Three focused Chromium cases pass for dashboard refresh/state transitions, exact 10,000-page localized pagination over 100k rows, continuous virtualization with `aria-rowcount=100001`, and the empty table contract. The seven existing DataTablePage cases also pass after the extension. This completes the dashboard/table gallery checklist line; calibrated benchmark artifacts, other gallery shapes, hostile content, and broad visual matrices remain separate.
- Added `/gallery/list-detail`, `/gallery/settings`, `/gallery/form`, and `/gallery/reading` behind one explicit ready/empty/loading/server-error/permission state contract. Delayed refresh keeps each accepted composition and its uncontrolled draft connected, visible, and opaque across twenty sampled frames. Application checks cover list arrow navigation, settings dirty/save behavior, app-owned form validation and keyboard checkbox activation, and semantic reading navigation. The first browser runs exposed a missing AppShell owner plus eager JSX embedded in fixture/config objects for branches omitted by SSR; moving ownership to the rendered branch removed the hydration exceptions instead of masking them. Three focused Chromium cases and repository lint/typechecks pass. The grouped gallery TODO remains open only for the command-palette overlay dependency.
- Added the dependency-free `/gallery/hostile` pressure surface with exact seeded boundaries: a 500-character unbroken value, zero-width characters, multilingual/RTL/emoji text, 100,000 continuous client rows, and 200 menu records across seven submenu levels. Two fixture tests and three Chromium cases pass. They verify desktop and phone overflow bounds, virtualization, the twenty-card mobile page, keyboard traversal to the deepest action, and a viewport-bounded 193-action terminal menu. Twelve-series streaming remains explicitly pending rather than represented by a false static substitute, so the hostile checklist line remains open.

## Theme-editor progress

- Added `/theme-editor` over the public token compiler. It edits every semantic token, exposes OKLCH channels for colors, derives either mode from an opaque background plus one of twelve accents and a neutral hue, and reports live contrast/chart-palette failures without hiding invalid intermediate work. Export remains fail-closed through `defineTheme`.
- The editor owns one scoped style element appended last, collapses writes to one animation frame, then invokes the Loupe-only chart-token invalidation bridge. A marked preview, uncontrolled field draft, and canvas remain mounted and opaque through derivation and direct token edits. Work in progress is versioned in guarded localStorage and recovered only after an opaque dark SSR/hydration boundary, so stored state cannot cause an incorrect default preview flash.
- Plain `ThemeDefinition` JSON and Loupe's exact generated `*.theme.ts` envelope round-trip through JSON parsing and strict validation; arbitrary TypeScript is rejected and never evaluated. Four focused model tests, repository lint/typechecks, and three real Chromium cases pass, including download contents, persisted reload recovery, retained canvas pixels, twenty-frame opacity sampling, and delayed JavaScript. The M4 base-theme/editor checklist line is complete; the token explorer, WCAG/APCA matrix, other engines, and full responsive/visual matrices remain separate.

## Token-explorer progress

- Added `/tokens` with all tier-1 primitives and tier-2 semantic tokens, computed browser values, visual previews, and live filtering by name, variable, or manifest component. `pnpm manifest` now emits an 11KB consumer map instead of making Loupe ship the 9.8MB exhaustive manifest. Theme/accent/mode changes flow through the existing rAF token bridge and retain row identity.
- Added every semantic foreground-by-background combination with translucent-background compositing and diagnostic WCAG 2.2 ratios, plus all eight chart colors under normal, protanopia, deuteranopia, tritanopia, and achromatopsia rendering. Three focused model tests and three Chromium cases pass, including delayed-JavaScript retention of server rows before computed values arrive.
- The official APCA implementation is still beta and carries integration, naming, polarity, attribution, and example-display conditions. Loupe links those primary requirements and leaves signed perceptual Lc output visibly pending rather than shipping an untracked port. The grouped token-explorer TODO stays unchecked until the official `apca-w3` dependency and compliant UI are approved and implemented.

## Chart rendering and streaming progress

- Added the bounded SVG chart layer with real `NaN` path breaks, separately accumulated positive/negative stacked areas, and grouped or stacked bars in vertical and horizontal layouts over an explicit categorical contract. Hydration starts from deterministic 640-pixel coordinates, then a frame-coalesced observer matches coordinates to the actual viewport without stretching text. Indexed reconciliation retains surviving paths and bars across resize and refresh. Seven Chromium cases cover geometry, native tables, twenty pending frames, rejection, delayed hydration, phone resize, axe, and a reviewed baseline. Chrome DevTools reports accessibility and best practices 100, no console warnings/errors, 77 ms local LCP, and zero CLS.
- The isolated D3 entry is 24,521 bytes gzip including shared chart CSS against a 35,000-byte gate, and excludes uPlot. The categorical conversion/validation contract is now explicit in `SPEC.md` and `docs/chart-data.md`.
- Added the renderer-free `useStreamingSeries` owner with a preallocated circular write buffer, immutable chronological frame snapshots, minimum publication cadence, batch validation, and exact distinction between unseen producer overflow and normal presented-history eviction. Out-of-order samples have a separate counter. Solid disposal cancels pending work and makes further mutation fail closed. Six focused unit tests and five Chromium cases cover real 60 Hz input, synchronous overflow, renderer/table identity, delayed hydration, disposal, and axe. The isolated streaming entry is 2,346 bytes gzip and retains neither chart engine.
- Completed the chart interaction layer. TimeSeries now has synchronized retained DOM tooltips, a keyboard inspection cursor, annotations that track redraw and zoom, pressed legend toggles, drag and toolbar zoom, double-click reset, and accepted-refresh zoom retention. SVG line/area charts use a retained crosshair; vertical and horizontal bars use a retained category band. Each plot is one tab stop with Left/Right, Home/End, and Escape, while the native table remains the complete equivalent. A broad hidden-series selector discovered by the browser test no longer hides the very legend control needed to restore that series. Twelve focused unit cases and nineteen Chromium cases pass across TimeSeries and SVG interaction, refresh, hydration, responsive, visual, and accessibility coverage; both reviewed chart baselines remain clean.
- Added the calibrated chart benchmark and CI artifact for four seeded series across 100,000 points, 120 retained tooltip updates, a two-second drag zoom, 120 streamed samples at 60Hz, Sparkline mount, and a simultaneous mode/accent redraw across twenty canvases. Profiling exposed repeated immutable-column scans and an oversized first enhancement task. Typed-array analysis is now cached at the validation boundary, fallback decimation walks finite segments rather than all points, and canvas enhancement begins after the fallback's first paint. The accepted repeat measured 60.2ms for complete progressive enhancement, 1.6ms for Sparkline mount work, and 45.7ms for twenty chart redraws; continuous workloads stayed at or below 16.8ms p99 with no Long Tasks, frames over 50ms, unexpected shift, lost samples, or replaced owners. The local baseline is diagnostic until replaced by the first accepted pinned `ubuntu-24.04` artifact.

## Token explorer APCA progress

- Completed the semantic foreground/background matrix with signed APCA 0.0.98G-4g Lc diagnostics alongside the existing WCAG 2.2 ratios. Foreground alpha is composed over the resolved opaque background before the polarity-sensitive calculation. APCA remains explicitly diagnostic; WCAG role gates remain the conformance boundary.
- Loupe uses Color.js 0.7.1 through its functional API rather than the restrictive, stale direct `apca-w3` package. Color.js is MIT licensed, current, zero-dependency, and the route-split integration stays outside every published Sheen package. Four unit cases and three Chromium cases cover known polarity, live theme updates without row replacement, and delayed hydration. A Chrome DevTools live pass found 588 resolved cells, no pending scores, no form-field issue after the hidden Select owner gained a stable ID, and Lighthouse accessibility/best-practices scores of 100.

## Application-shape gallery progress

- List-detail, multi-section settings, form-heavy workflow, and long-form reading scenarios now share a scoped command palette with refresh and all operational-state commands. `mod+k` and the visible trigger open the same contextual overlay; refresh closes it and retains the accepted content owner and native drafts while the pending revision resolves.
- Eight focused Chromium cases pass across all four shapes, including twenty-frame retained refresh sampling, application-specific semantics, palette-driven refresh, authorization clearing, and a queued pre-hydration refresh with an adopted native draft.

## Hostile gallery progress

- The hostile route now combines the existing 500-character token, zero-width controls, multilingual/RTL copy, seven nested menu levels with 200 records, and virtualized 100,000-row continuous table with a real twelve-series `useStreamingSeries` owner. The first eight signals consume the categorical palette; the remaining four reuse colors with explicit dashed, dotted, and dash-dot encodings.
- Added validated non-color encoding to the shared chart series contract and carry it consistently through the deterministic fallback, uPlot canvas, bounded SVG lines/areas, bar outlines, tooltips, and legends. Nineteen focused unit cases and twenty-two combined hostile/chart Chromium cases pass. The two intentional chart visual changes were inspected and accepted as the new baselines.

## Cross-system qualification matrix progress

- `tests/browser/qualification-matrix.spec.ts` pins four reviewable application cases spanning all AdminApp presets, desktop/tablet/phone widths, LTR/RTL, and dark/light modes. Each case checks bounded layout, keyboard reachability, axe A/AA, and a viewport screenshot.
- `tests/browser/contrast.spec.ts` remains the exhaustive palette gate: six themes by two modes by twelve accents, separated from the bounded screenshot set.
- The rationale and exact matrix live in `docs/qualification-matrix.md` so additions remain deliberate instead of becoming a Cartesian screenshot explosion.

## Cross-engine hydration and refresh progress

- The same delayed-font, delayed-JavaScript, and delayed-data test now passes in Playwright Chromium and WebKit. It starts from a stored Slate/light/Rose preference on the dark-default app and requires the accepted content, native draft, DOM identities, and root theme to survive every sampled frame.
- The pinned Nix environment now provides WebKit's GTK, WPE, media, text, image, and ABI-specific runtime libraries. Playwright's bundled Linux wrapper replaces `LD_LIBRARY_PATH`, so the WebKit config bypasses that wrapper only when `SHEEN_NIX_WEBKIT=1`, launches the same bundled executable, and combines its private libraries with the pinned Nix runtime. Hosted CI retains the ordinary Playwright launcher.
- Offline persistence assertions now check the portable visible-failure contract rather than V8's engine-specific `Failed to fetch` wording. The focused theme/hydration group passes nine cases in Chromium; the same nine pass in WebKit after that correction.

## Automated accessibility inventory progress

- `tests/browser/accessibility.spec.ts` derives its base inventory directly from every Loupe route module, with explicit representative URLs for dynamic routes. Unknown dynamic route shapes fail inventory construction instead of silently losing coverage.
- The inventory adds ready, empty, cold-loading, server-error, and permission-denied application states plus open dialogs, drawers, menus, selectors, calendars, toasts, table edit failures, and AdminApp account, notification, command, and details layers.
- All 169 Chromium cases pass the axe WCAG A/AA tags. The audit exposed and closed undersized fixture targets, an unnamed token-matrix scroll region, and an incorrectly placed combobox scroll owner. The editable combobox retains DOM focus on its input per the WAI-ARIA pattern; its controlled listbox now owns overflow and active-option scrolling.

## Application-readiness cross-engine progress

- Expanded the pinned WebKit gate from the initial shell/theme slice to 212 release-critical cases spanning every AdminApp preset and legal semantic placement, Composer, integrated DataTable search/filter/sort/edit/export/selection/views/URL contracts, paginated and continuous mobile presentation, date/time controls, overlays, toasts, shell scroll restoration, hydration, RTL, reduced motion, and non-flashing refresh. The accepted Nix-shell run passed 210 in 2.3 minutes; the two explicit skips are a Chromium-owned raster baseline and deterministic page-inactivity emulation, which Playwright exposes only through Chromium CDP. Physical Safari/VoiceOver and NVDA/Firefox remain manual release gates.
- Cross-engine pressure exposed a focus-restored tooltip that WebKit could reopen after Escape while the trigger remained engaged, allowing it to cover an adjacent action. Tooltip dismissal now remains latched until blur or pointer leave. It also exposed three test timing assumptions: WebKit wheel inertia could continue after an early sample, request duration was compared with a frame count, and axe could inspect partially transparent enter animations. Deterministic scroll placement, a held real endpoint request, and finite-animation settling now test the intended contracts without mocks.
- A live Chrome DevTools review of the bound `0.0.0.0:4173` AdminApp and Composer reports accessibility and best-practices scores of 100 for both. The default AdminApp accessibility tree contains the complete branded shell, configurable navigation/account/action chrome, integrated searchable/sortable/filterable table, pagination, status, and overlay owners. Its local unthrottled development trace measured 148ms LCP and 0.00 CLS. A clean Composer reload has no failed resources or runtime errors; only Vite development font-preload warnings remain.
