// Generated by tools/build-manifest.ts. Do not edit.
import { Show } from "solid-js";
import type { ComponentProps } from "solid-js";
import { DataTable, FilterBar, QueryBuilder, parsePagination, resolvePagination, resetPagination, resizePagination, getPageRange, paginateClientRows, assessServerPage, createTableRequests, parseFilter, serializeFilter, deserializeFilter, queryBuilderFilterColumns, defaultQueryRule, queryNodeAt, replaceQueryNode, appendQueryChild, removeQueryNode, moveQueryNode, toggleQueryNegation, listFilterConditions, replaceFilterCondition, removeFilterCondition, appendFilterCondition, removeFilterColumnConditions, compileFilter, filterClientRows, parseSorting, sortClientRows, countClientFacets, formatSearchQuery, parseSearchQuery, searchClientRows, createDebouncedSearch, createClientView, parseClientViewState, changeClientViewState, parseTableState, serializeState, deserializeState, changeTableState, orderColumnStates, placeColumn, moveColumn, canMoveColumn, createTableSelection, createClientExport, selectClientRows, createServerExport, createCellEdit, defineColumns, createRowIdentity, groupClientRows, normalizeServerGroups, createRowHierarchy, createTableUrlState, createSavedViews, englishTableMessages, resolveTableMessages } from "@gemologic/sheen-table";
import { createSignal } from "solid-js";
import type { FilterNode, QueryBuilderColumn } from "@gemologic/sheen-table/core";
import type { ComponentExampleModule, PlaygroundValue } from "./index.ts";

function Playground(props: { readonly values: Readonly<Record<string, PlaygroundValue>> }) {
  return <Show keyed when={props.values}>{currentValues => {
    const queryColumns = [
  { id: "name", label: "Name", type: "text" },
  { id: "status", label: "Status", type: "enum", options: ["active", "paused"] },
] satisfies readonly QueryBuilderColumn[];
const [filter, setFilter] = createSignal<FilterNode>({ kind: "and", children: [
  { kind: "text", column: "name", operator: "contains", value: "Acme", caseSensitive: false },
] });
      const value0 = currentValues["label"];
      const controlled0 = typeof value0 === "string" ? value0 : undefined;
      const value1 = currentValues["disabled"];
      const controlled1 = typeof value1 === "boolean" ? value1 : undefined;
    const controlledProps = {
        ...(controlled0 === undefined ? {} : { "label": controlled0 }),
        ...(controlled1 === undefined ? {} : { "disabled": controlled1 })
    } satisfies Partial<ComponentProps<typeof QueryBuilder>>;
    return (<QueryBuilder columns={queryColumns} value={filter()} onChange={setFilter} label="Account query"  {...controlledProps}/>);
  }}</Show>;
}

export const exampleModule = {
  defaults: Object.freeze({"label":"Query builder","disabled":false}),
  controlsSupported: true,
  playgroundTemplate: "<QueryBuilder columns={queryColumns} value={filter()} onChange={setFilter} label=\"Account query\" />",
  codeInsertionOffset: 97,
  playground: Playground,
  examples: Object.freeze([
    {
      title: "Nested account query",
      code: "<QueryBuilder columns={queryColumns} value={filter()} onChange={setFilter} label=\"Account query\" />",
      render: function Example0() {
        const queryColumns = [
  { id: "name", label: "Name", type: "text" },
  { id: "status", label: "Status", type: "enum", options: ["active", "paused"] },
] satisfies readonly QueryBuilderColumn[];
const [filter, setFilter] = createSignal<FilterNode>({ kind: "and", children: [
  { kind: "text", column: "name", operator: "contains", value: "Acme", caseSensitive: false },
] });
        return (<QueryBuilder columns={queryColumns} value={filter()} onChange={setFilter} label="Account query" />);
      },
    }
  ]),
  variants: Object.freeze([

  ]),
} satisfies ComponentExampleModule;
