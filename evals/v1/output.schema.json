{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "required": ["code", "notes"],
  "properties": {
    "code": {
      "type": "string",
      "minLength": 1,
      "description": "One complete TSX module implementing the prompt."
    },
    "notes": {
      "type": "string",
      "description": "Concise assumptions and application-owned integration boundaries."
    }
  }
}
